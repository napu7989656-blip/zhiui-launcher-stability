# 智UI交付与测试记录规则

## 适用范围

本规则适用于智UI的每一次功能调整、修复和版本交付。

## 验证通过后的处理

1. 仅当最新版 APK 已完成目标功能的实际验证并确认通过后，才可作为交付包。
2. 将该次验证通过的最新版交付 APK 移动到：

   ```text
   D:\output\zhiui\clear
   ```

3. `clear` 目录中的交付 APK 只保留按验证通过时间排序的最近三个版本。
4. 当移动新的已验证交付包后，如交付 APK 超过三个，删除其中最早的版本；不删除交接文档。
5. 未完成验证、仅完成打包或仅完成签名校验的 APK 不得移动到 `clear` 目录。

## 测试记录

每次验证通过后，必须将本次测试内容追加写入以下统一交接文档：

```text
D:\output\zhiui\clear\智UI交接文档.md
```

每次记录至少包含：

- 验证日期与时间；
- 交付包文件名、版本说明和 SHA-256（如可取得）；
- 本次调整内容；
- 测试设备/环境；
- 实际验证步骤与结果；
- 已知限制、风险及未验证项；
- 验证结论（通过/不通过）。

## 交付判定

- “已验证通过”指已在目标车机或约定测试环境完成与调整项相对应的实际验证。
- 签名校验、可安装、可启动不能单独代表功能验证通过；应在交接文档中分别记录。
- 如后续发现回归问题，应在交接文档中追加问题记录，并停止将该版本作为推荐交付包。
