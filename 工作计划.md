# 智Ui 方控单区空调弹窗工作计划

## 1. 目标与边界

在智Ui中新增一个由**方控模式按钮**调出的单区空调弹窗，视觉和交互结构参考 `dd.apk` 的空调弹窗。

本次范围：

- 仅支持单区空调；
- 不提供副驾温区、SYNC、后排空调；
- 不提供座椅加热、座椅通风；
- 不在状态栏、导航栏、桌面增加任何空调入口；
- 只能由方控的“模式”按键触发显示或关闭；
- 弹窗打开后允许触摸操作；
- 使用智Ui现有的车辆 IPC 指令，不移植 `dd.apk` 的车机协议、代码或资源。

## 2. 现状依据

智Ui已经具备以下基础能力：

- 方控按键映射保存在 `launcher_prefs` 的 `swc_mapping_<keyCode>`；
- `NavBarService.onKeyEvent()` 已负责接收和分发方控按键；
- 方控当前已有 `ac_wind_up`、`ac_wind_down`、`ac_temp_up`、`ac_temp_down`、`ac_toggle` 等空调动作；
- 空调状态通过 `getAcState` 获取；
- 已存在 `setAcTemperature`、`setAcWindLevel`、`setAcDefrostState`、`startAc`、`stopAc` 等控制链路；
- `NavBarService` 已具备跨应用显示 Overlay 的 `WindowManager` 能力。

`dd.apk` 的空调入口由状态栏项触发，内部存在不同车型版本和双区/座椅等扩展能力。本方案只借鉴其单区弹窗的布局层级、温度滚轮和状态反馈，不复制其实现。

## 3. 方控模式键方案

### 3.1 新增映射动作

新增方控动作值：

```text
ac_popup = 空调控制
```

### 3.2 设置页规则

在“方控按键设置”中：

1. 找到现有“模式”物理按键对应的映射项；
2. 只在该项的下拉选项中加入“空调控制”；
3. 其他物理按键不出现“空调控制”选项；
4. 原有空调温度、风量、总开关快捷动作保留，但归为“空调相关快捷功能”，不作为弹窗入口。

### 3.3 按键行为

| 条件 | 行为 |
|---|---|
| 模式键映射不是 `ac_popup` | 保持原有行为 |
| 模式键映射为 `ac_popup` 且弹窗未显示 | 打开空调弹窗并拉取状态 |
| 模式键映射为 `ac_popup` 且弹窗已显示 | 关闭空调弹窗 |
| 长按重复事件 | 忽略，防止连续开关 |
| 当前处于倒车等受限场景 | 不显示或立即关闭 |

只处理 `ACTION_DOWN` 且 `repeatCount == 0` 的首次按下事件；被识别为 `ac_popup` 后消费事件，避免模式键再传递给系统或其他应用。

## 4. 弹窗架构

弹窗由 `NavBarService` 管理，不挂在状态栏、导航栏或桌面入口上。

```text
方控模式键
    ↓
NavBarService.onKeyEvent()
    ↓  swc_mapping = ac_popup
AirPopupManager.toggle()
    ↓
WindowManager Overlay
    ├─ 全屏半透明遮罩
    └─ 单区空调控制面板
```

选择 Overlay 的原因：方控按键由服务接收，且该方案可在桌面、地图和其他应用前台时正常调出面板。

弹窗背景仅使用半透明纯色、圆角和渐变，不读取或缓存动态壁纸视频帧，避免额外 GPU→CPU 拷贝、内存分配和画面冻结问题。

## 5. UI 方案

整体模仿 `dd.apk` 的空调弹窗风格，但按智Ui主题重新制作：深色半透明面板、圆角容器、大字号温度、图标状态区和明确的激活态。

```text
┌──────────────────────────────────┐
│              空调控制             │
│                                  │
│           ┌──────────┐           │
│           │  24.0 ℃  │           │
│           │  温度滚轮 │           │
│           └──────────┘           │
│                                  │
│       风量  ────────  0 ～ 7       │
│                                  │
│  [空调开关] [内循环] [前除霜]      │
│  [后除霜]   [A/C*]   [AUTO*]       │
└──────────────────────────────────┘
```

`*` 项仅在车辆控制层确认支持并能返回真实状态时显示。

### 5.1 第一版固定功能

- 空调总开关；
- 单区目标温度；
- 风量 0～7；
- 内循环；
- 前除霜；
- 后除霜。

### 5.2 延后启用功能

- A/C；
- AUTO；
- 风向切换。

这些功能先完成 UI 预留与能力开关；只有确认 IPC 指令、状态字段和实车反馈都存在时才开放。

### 5.3 关闭规则

- 再按一次模式键；
- 点击遮罩空白区；
- 触发返回键；
- 进入倒车、相机等高优先级全屏场景；
- 服务销毁、屏幕关闭或窗口异常时。

## 6. 状态与指令设计

新增 `AirConditionRepository`（或等价状态控制器），统一维护空调真实状态、能力开关、请求状态和错误信息。

```text
getAcState / 车辆状态通知
             ↓
AirConditionRepository
             ↓
AirPopupManager / 空调弹窗 UI

用户点击或滚轮结束
             ↓
AirCommandDispatcher
             ↓
CoreServiceClient
             ↓
车辆服务确认 → getAcState 回写 UI
```

规则：

1. 打开弹窗时立即请求 `getAcState`；
2. 控件显示以最近一次车辆返回状态为准；
3. 用户操作后可显示短暂“下发中”状态，但不得永久乐观更新；
4. 收到车辆成功状态后再确认 UI；
5. 超时、失败或服务断开时恢复最后一次真实状态并提示；
6. 温度滚轮、风量连续调节应做防抖：滑动停止后发送最终值，避免连续堆积指令。

## 7. 实施步骤

### 阶段 A：验证车辆能力

1. 记录 `getAcState` 的完整返回字段和字段变化；
2. 逐项验证总开关、温度、风量、循环、前后除霜；
3. 确认 A/C、AUTO、风向是否存在可调用指令及状态回传；
4. 输出单区车型能力表，未确认能力默认隐藏。

验收：每一个计划显示的控件都有对应的真实状态字段与可验证指令。

### 阶段 B：扩展方控映射

1. 将 `ac_popup` 加入方控动作枚举和展示文案；
2. 修改方控设置 UI，使“空调控制”只出现在模式键映射项；
3. 保持既有映射数据兼容，升级后不重置用户设置；
4. 在 `NavBarService.onKeyEvent()` 增加 `ac_popup` 分支。

验收：模式键可配置为“空调控制”；其他按键不可配置该入口；旧映射不受影响。

### 阶段 C：实现弹窗生命周期

1. 新建 `AirPopupManager`；
2. 创建与现有服务权限一致的 Overlay 窗口参数；
3. 实现打开、关闭、重复按键切换、遮罩点击和异常清理；
4. 接入倒车/全屏场景、熄屏和服务销毁的强制关闭逻辑；
5. 确保同一时间只存在一个空调弹窗实例。

验收：任意前台应用中均能由模式键稳定调出和关闭，不残留窗口。

### 阶段 D：实现单区空调 UI

1. 新建单区空调弹窗布局、圆角背景和图标状态资源；
2. 实现温度滚轮与温度文本显示；
3. 实现风量条/风扇等级、总开关、循环、前后除霜；
4. 按能力开关动态显示 A/C、AUTO、风向；
5. 所有图标和文本需具备启用、禁用、加载、失败四种状态。

验收：视觉结构接近 DD 空调弹窗；但无双区和座椅区域，布局不留空白功能位。

### 阶段 E：接入状态与控制

1. 实现 `AirConditionRepository` 和指令分发器；
2. 接入 `getAcState`、温度、风量、除霜、总开关等现有 IPC；
3. 为每项指令增加回调、超时和重新拉取状态逻辑；
4. 对暂不支持的功能只展示禁用或隐藏，不发送猜测性指令。

验收：所有可见控件都与实车状态同步；失败不会留下错误的 UI 状态。

### 阶段 F：回归测试

1. 方控映射：启用/禁用自定义映射、模式键映射切换、旧映射兼容；
2. 按键：单击、长按、连按、弹窗开关切换；
3. 空调：开关、温度边界、风量边界、循环、前后除霜；
4. 生命周期：熄屏亮屏、服务重连、前后台、横屏恢复；
5. 场景：桌面、地图、倒车、相机和其他全屏应用；
6. 性能：持续打开/关闭弹窗，无窗口泄漏、无动态壁纸截图、无明显掉帧。

## 8. 验收标准

- 用户可在方控设置中把“模式”按钮设为“空调控制”；
- 不存在状态栏、导航栏、桌面图标或触摸入口；
- 模式按钮是唯一可调出弹窗的入口；
- 弹窗为单区空调，不显示副驾、后排、SYNC、座椅加热或通风；
- 所有显示的控制功能都可获得车辆真实状态并完成实车验证；
- 弹窗在各前台应用中可正常显示和关闭，特殊高优先级场景不会遮挡；
- 不读取动态壁纸帧，不产生静态截图背景或额外视频渲染负担。
