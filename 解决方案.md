# 智 UI 跨应用状态栏问题解决方案

## 1. 文档目的

本文用于交接 `zhiui-final-v2.apk` 的状态栏问题分析和后续修复方案。

现象：智 UI 主界面的顶部状态栏显示正常；打开其他 App 后，智 UI 状态栏消失，随后显示比亚迪原车 `SystemUI` 状态栏。

本轮只进行了 ADB 只读检查和 APK 静态分析，没有修改 APK、没有修改车机设置、没有启动其他 App，也没有调用任何车辆控制接口。

## 2. 检查对象

- 当前车机包名：`com.byd.launcher`
- 当前安装 APK：`/data/app/com.byd.launcher-LbX4Mj2xWgA_E0G-frAZxw==/base.apk`
- 车机 APK SHA-256：`ADA7BD1DA144470F8C05BD24F0A7D8A27D00C435C0146D517F455D9F112FF1DC`
- 本地 `D:\apk\zhiui-final-v2.apk` SHA-256：`ADA7BD1DA144470F8C05BD24F0A7D8A27D00C435C0146D517F455D9F112FF1DC`

车机安装包与本地 v2 文件完全一致，本分析针对的就是当前实车版本。

静态分析工作树：

- apktool：`D:\apk\launcher_project\diagnostics\zhiui_final_v2`
- 原始 v2 APK：`D:\apk\zhiui-final-v2.apk`

这些目录只用于分析，不得覆盖 `ORIGINAL_BASELINE.apk` 或现有 v2 APK。

## 3. ADB 现场结论

### 3.1 Launcher 和服务没有崩溃

- `com.byd.launcher` 进程正常运行。
- `com.byd.launcher.NavBarService` 已作为 AccessibilityService 启用并绑定。
- 智 UI 底部导航栏的无障碍悬浮窗口仍然存在。
- 底部悬浮窗口类型为 `2032`，即 `TYPE_ACCESSIBILITY_OVERLAY`。
- 窗口范围为 `(0,990)-(1920,1080)`，高度 90px。

因此，本问题不是 Launcher 进程退出，也不是整个 `NavBarService` 被系统杀死。

### 3.2 系统状态栏策略

车机当前的全局策略为：

```text
immersive.status=com.byd.launcher
```

这条策略只在 `com.byd.launcher` 位于前台时隐藏原车顶部状态栏。打开其他 App 后，包名不再匹配，`com.android.systemui` 的原车状态栏会恢复显示。

该策略本身不会让智 UI 状态栏跨应用显示；它只负责隐藏或显示原车状态栏。

## 4. 已确认根因

### 4.1 v2 禁用了全局悬浮状态栏

文件：

```text
D:\apk\launcher_project\diagnostics\zhiui_final_v2\smali\com\byd\launcher\NavBarService.smali
```

方法：

```smali
.method public final Ϡ(I)V
```

该方法从第 27706 行开始，在第 27713 行直接执行：

```smali
return-void
```

所以该方法后面原本完整存在的以下代码全部成为不可达代码：

- inflate `status_bar.xml`
- 创建顶部 `WindowManager.LayoutParams`
- 使用 `TYPE_ACCESSIBILITY_OVERLAY`（2032）
- 把状态栏加入 WindowManager
- 初始化蓝牙、网络、音乐、通知等状态控件
- 执行状态栏重新附着和恢复逻辑

APK 内仍有两个位置调用 `Ϡ(I)`，分别约在第 26563 和 26955 行。调用链没有被删除，真正阻断状态栏创建的是方法入口的无条件返回。

### 4.2 v2 把状态栏改成了 MainActivity 的普通子 View

文件：

```text
D:\apk\launcher_project\diagnostics\zhiui_final_v2\res\layout\activity_main.xml
```

第 5 行加入：

```xml
<include
    android:layout_gravity="top"
    android:layout_width="match_parent"
    android:layout_height="48.0dp"
    layout="@layout/status_bar" />
```

这使 `status_bar.xml` 只属于 `MainActivity`：

```text
智 UI 位于前台
→ MainActivity 可见
→ 内嵌状态栏可见

打开其他 App
→ MainActivity 被遮挡
→ 内嵌状态栏随 Activity 一起消失
→ 原车 SystemUI 状态栏恢复显示
```

这与实车现象完全一致。

### 4.3 全屏应用判断还会主动隐藏智 UI 栏

`NavBarService.onAccessibilityEvent()` 会读取前台 Activity，并检查：

- Activity 是否声明 `supports_fullscreen=true`
- 是否位于 `fullscreen_packages`
- 是否位于 `statusbar_hidden_packages`
- 内置 Activity/包名全屏集合

当前实现把同一个 `supports_fullscreen` 结果同时用于底部导航栏和顶部状态栏。当某个 App 声明全屏支持时，代码会把相关 View 的 alpha 设为 0，并将触摸覆盖层设为 `GONE`。

这不是所有 App 都发生切换的首要根因，但修复全局悬浮状态栏后，如果不拆分该判断，部分导航、视频或原车 App 仍可能主动隐藏顶部状态栏。

## 5. 推荐修复方案

建议创建独立版本：

```text
zhiui-final-v3-statusbar-fix.apk
```

只能以 `zhiui-final-v2.apk` 的独立解码副本为修改基线，不得覆盖：

- `ORIGINAL_BASELINE.apk`
- `zhiui-final-v2.apk`
- `Launcher_Rebuild_Test.apk`
- 现有开发 keystore 和实车回滚包

### 5.1 恢复 NavBarService 的全局悬浮状态栏

修改 `NavBarService.smali` 中的 `Ϡ(I)V`：

- 删除或绕过方法入口处新增的无条件 `return-void`。
- 保留方法后方原有的完整实现。
- 不重写整个方法，不复制 JADX 伪源码。
- 以 apktool smali 为行为事实来源。

原实现已经包含：

- `status_bar.xml` 加载
- 2032 无障碍悬浮窗口
- WindowManager 添加、异常处理和重新附着
- 状态图标初始化
- `ensureStatusBarReattached` 恢复逻辑

因此该修复不需要重新实现一套状态栏。

### 5.2 移除 MainActivity 内嵌状态栏

从 `activity_main.xml` 删除 v2 新增的 `@layout/status_bar` include。

原因：恢复 WindowManager 状态栏后，如果继续保留 Activity 内嵌版本，智 UI 主界面会出现两份状态栏，可能表现为图标重叠、点击区域冲突或内容重复刷新。

修复后只保留一套状态栏：

```text
NavBarService
→ WindowManager
→ TYPE_ACCESSIBILITY_OVERLAY
→ status_bar.xml
```

### 5.3 拆分顶部状态栏与底部导航栏的全屏策略

建议调整 `onAccessibilityEvent()` 的判断：

- `supports_fullscreen` 和 `fullscreen_packages` 可以继续控制底部导航栏。
- 不要再让 `supports_fullscreen` 自动隐藏顶部状态栏。
- 顶部状态栏只由 `statusbar_hidden_packages` 或明确内置黑名单控制。
- 对视频全屏、倒车影像等确实需要无遮挡的页面，保留按 Activity 精确隐藏能力。
- 不得将车辆控制页面作为测试对象。

推荐规则：

| 前台场景 | 智 UI 顶部状态栏 | 智 UI 底部导航栏 |
|---|---:|---:|
| 智 UI 主界面 | 显示 | 显示 |
| 普通第三方 App | 显示 | 显示 |
| 普通原车 App | 显示 | 显示或按现有规则处理 |
| 视频全屏页面 | 可按 Activity 黑名单隐藏 | 可隐藏 |
| 倒车影像/全景影像 | 不改现有安全逻辑，单独验证 | 不改现有安全逻辑，单独验证 |

### 5.4 处理原车 SystemUI 状态栏遮盖

智 UI 状态栏布局当前背景为透明。切换到其他 App 后，原车状态栏会位于它下方，可能出现两套图标叠加。

推荐方案：

- 智 UI 主界面前台：状态栏背景保持透明，与壁纸融合。
- 其他 App 前台：状态栏背景切换为黑色或半透明黑色，遮住底层原车状态栏。
- 保留系统原生 48px 状态栏区域，让第三方 App 继续获得正确的顶部 inset，避免 App 内容被悬浮栏遮挡。

不建议直接全局隐藏原车状态栏。使用覆盖层遮盖比修改所有 App 的沉浸策略更容易回滚，也不会改变第三方 App 的布局高度。

### 5.5 保留“使用原车状态栏”设置

现有偏好项：

```text
oem_statusbar_enabled
oem_real_statusbar_enabled
```

修复时应继续保留这两个设置，但默认使用智 UI 状态栏时必须确保它们不会把自定义悬浮状态栏设为 `GONE`。

如果用户主动选择原车状态栏：

- 隐藏智 UI 顶部悬浮栏。
- 清理或恢复相应 `policy_control`。
- 显示原车 SystemUI 状态栏。

## 6. 不推荐方案

### 6.1 不要使用全局沉浸命令代替修复

不建议：

```text
settings put global policy_control immersive.status=*
```

原因：

- 当前 v2 的全局智 UI 状态栏代码仍被入口 `return-void` 禁用。
- 执行后可能变成所有 App 都没有顶部状态栏。
- 会影响原车应用和第三方应用。
- 可能造成内容延伸到刘海、摄像头区域或系统交互区域。
- 属于全局系统配置，不符合最小修改和易回滚原则。

### 6.2 不要只复制一份状态栏到每个 Activity

这种方案不能覆盖第三方 App，也会产生多个状态栏实例和状态同步问题。

### 6.3 不要重写整个 NavBarService

原 APK 已经保留完整的 WindowManager 状态栏实现。应恢复并修正现有逻辑，不需要重写 Launcher 或堆叠大量新 smali。

## 7. 实施范围

建议本版本只修改：

```text
res/layout/activity_main.xml
smali/com/byd/launcher/NavBarService.smali
```

如果需要跨应用动态背景，可能再增加一个状态栏背景 drawable，或在前台包变化处设置背景颜色。

本版本不得修改：

- CoreService
- CoreServiceClient
- 车辆 JSON 路由
- 空调、车窗、门锁、灯光等控制代码
- CAN、ECU、BMS、动力、制动、转向、ADAS、安全气囊相关逻辑
- 原车 Launcher 和系统关键应用状态

## 8. 构建与静态验证

标准流程：

```text
zhiui-final-v2.apk 的独立 apktool 工作树
→ 单项状态栏修改
→ apktool build
→ zipalign
→ 项目开发证书签名
→ apksigner verify
→ aapt/apkanalyzer 解析检查
→ 输出 zhiui-final-v3-statusbar-fix.apk
```

静态验收项：

- APK 成功生成。
- zipalign 通过。
- V2/V3 签名验证通过。
- 包名仍为 `com.byd.launcher`。
- Manifest 组件没有无关变化。
- DEX 可解析。
- `NavBarService.Ϡ(I)` 不再入口直接返回。
- `activity_main.xml` 不再 include `status_bar.xml`。
- `status_bar.xml` 只由 NavBarService 创建一份实例。
- 原 v2 APK 和回滚包哈希不变。

## 9. 实车测试方案

所有测试必须在安全停车状态下由用户执行。未经当次明确授权，不通过 ADB 自动安装、卸载、清数据、设置默认 HOME 或重启车机。

### T01 安装和启动

- 安装修复包后启动智 UI。
- 确认没有闪退、黑屏或重复状态栏。
- 确认顶部状态栏和底部 Dock 均正常。

### T02 普通第三方 App

依次人工打开：

- 文件管理器
- 普通设置页面
- 一个不涉及车辆控制的第三方 App

预期：

- 智 UI 顶部状态栏持续显示。
- 不出现原车状态栏图标穿透或双层叠加。
- 底部导航栏行为不退化。

### T03 HOME 返回

- 从普通 App 按 HOME 返回智 UI。
- 重复两到三次。

预期：

- 状态栏不中断、不闪烁。
- 主界面不出现两份状态栏。
- 时间、网络、蓝牙等图标继续更新。

### T04 全屏应用

- 仅测试普通视频或导航页面，不进入车辆控制或驾驶安全相关页面。
- 验证黑名单策略是否按设计隐藏或保留顶部状态栏。

### T05 熄屏和亮屏

- 人工熄屏再亮屏两次。

预期：

- AccessibilityService 仍绑定。
- 顶部状态栏能够自动重新附着。
- 不出现透明触摸层残留。

### T06 稳定性

- 在智 UI 与普通 App 之间切换至少 10 次。
- 保持运行 15 分钟。

预期：

- `NavBarService` 不崩溃。
- WindowManager 不产生重复状态栏窗口。
- 没有持续的 `BadTokenException`、`View already added` 或 `View not attached`。

## 10. 建议采集的日志

发生异常时采集：

```powershell
adb logcat -v threadtime | Select-String -Pattern 'NavBarSvc|NavBarService|showStatusBar|ensureStatusBarReattached|WindowManager|BadTokenException|already added|not attached'
```

同时记录：

- 出问题的 App 包名和 Activity。
- 状态栏是否完全消失、变成原车样式或出现双层叠加。
- 底部导航栏是否仍在。
- HOME 返回后是否恢复。
- 问题截图和发生时间。

## 11. 回滚方案

修复包实车异常时：

- 停止继续测试。
- 不清除应用数据。
- 不删除原车 Launcher。
- 不更改默认 HOME。
- 在用户明确确认后，使用同一开发证书的 `D:\apk\zhiui-final-v2.apk` 覆盖回滚。
- 若无法覆盖安装，先收集安装错误，不擅自卸载现有 Launcher。

已验证回滚包：

```text
D:\apk\zhiui-final-v2.apk
SHA-256: ADA7BD1DA144470F8C05BD24F0A7D8A27D00C435C0146D517F455D9F112FF1DC
```

## 12. 最终结论

该问题不是比亚迪系统不允许第三方状态栏跨应用显示，也不是 AccessibilityService 被系统杀死。

直接原因是 v2 主动禁用了 NavBarService 原有的全局悬浮状态栏，并把同一布局嵌入了 Launcher 的 MainActivity。恢复现有 WindowManager 状态栏、删除 Activity 内嵌副本，并拆分顶部状态栏与底部导航栏的全屏隐藏规则，即可从结构上解决问题。
