# 比亚迪海豚智UI Launcher 项目规则

本文件适用于 `D:\apk` 整个工作区。后续任何会话、代理和脚本都必须先遵循本文件，再开展工作。

## 1. 项目目标与目标环境

- 目标车辆：比亚迪海豚 2021 款时尚版。
- 目标车机版本：`13.1.22.2409213.1`。
- 工作方式：分析既有第三方 Launcher APK，在既有实现上做可回滚的增量修改；不得把项目擅自改成全新 Launcher 重写。
- JADX 输出仅用于理解伪源码，不等于原始 Java/Kotlin 源码。涉及准确行为时必须和 apktool 的 Manifest、资源及 smali 交叉确认。

## 2. 不可变基线与必须保留的成果

- 权威基线：`ORIGINAL_BASELINE.apk`。
- 基线 SHA-256：`93042E48C12ED395E8F5D450BE1285E0397897F685EE95AC4080763B6E6DCCD9`。
- `original.apk` 与 `智UI_V0.4.2.apk` 和权威基线字节级相同，均不得覆盖或删除。
- `zhiui-final-v2.apk` 是既有、已实车验证的用户成果，不得被基线或测试包覆盖。
- 所有 keystore、实车截图、现有日志、`D:\output\zhiui\clear` 内交付物以及其他归档包都必须保留。
- 新分析、修改和构建只能进入独立目录或使用新文件名；任何覆盖行为都必须明确限定目标并保留回滚包。

## 3. 当前阶段门禁

- `Launcher_Rebuild_Test.apk` 目前只完成静态重建、解析、zipalign 和签名验证，尚未由用户确认实车安装与运行。
- 在用户明确反馈该零修改重打包版本可正常安装、启动、成为 HOME 并完成基本功能测试前，不开始正式 UI 或功能修改。
- 若零修改版本实车失败，先定位签名、资源、Manifest、DEX、native library、系统权限、完整性校验或防篡改原因；不得直接转为重写 Launcher。

## 4. 分析与修改原则

- 首次或结构变化后必须同时使用 JADX 与 apktool 审计，并维护 `PROJECT_MAP.md`。
- 修改优先级：资源/XML/drawable/string/dimen/theme/Manifest，其次是小范围、可审查的 smali；复杂逻辑应先评估是否适合实现成可维护的自有模块再集成。
- 一次只改一个主要功能，使用清晰版本号和独立 APK 文件名。每次都记录改动文件、构建结果、签名结果、实车测试项和回滚方式。
- 不得将 JADX 生成的 Java 直接当作可编译原源码或事实依据。
- 原始 APK 含大小写仅有差异的混淆资源路径；不得在 Windows/NTFS 上用普通 ZIP 解压作为修改工作树，只能通过 apktool 解码并检查日志。

## 5. 实车与车辆控制安全红线

- 允许研究 Launcher UI 和只读车辆状态；不得主动修改或发送 CAN、ECU、BMS、动力、制动、转向、ADAS、安全气囊或其他底层控制指令。
- 涉及实车测试时，先给出最小测试步骤、预期结果、失败回滚和需要采集的 logcat；默认由用户在安全停车状态下执行。

## 6. 签名与构建

- 标准流程：基线备份 → apktool/JADX 分析 → 单项修改 → apktool build → zipalign → 项目开发密钥签名 → apksigner/aapt/apkanalyzer 验证 → 用户实车测试。
- 构建脚本位于 `launcher_project\build.ps1` 和 `launcher_project\build.bat`，工具使用固定绝对路径，不能依赖当前 PATH。
- 开发测试包使用项目本地开发证书；原包证书不可用于重新签名。不得在公开文档、回复或公共仓库中暴露 keystore 密码或私钥。
- 换签后不能直接覆盖安装原证书版本；与 `zhiui-final-v2.apk` 使用同一开发证书的包可在版本规则允许时互相覆盖。任何卸载动作仍需用户明确确认。
- 构建成功、可安装和签名通过不代表功能验证通过。未实车验证的 APK 不得进入 `D:\output\zhiui\clear`。

## 7. 文档与交付

- 每轮分析或修改后维护 `PROJECT_MAP.md`：APK 信息、组件、页面/布局/类/资源映射、BYD/DiLink 接口、权限、安全风险、签名、构建状态和版本记录。
- 汇报要用用户可理解的功能描述，同时附关键文件位置；不能只给 smali 文件名。
- 实车失败时优先收集实际表现、截图和定向 logcat，再做下一项修改。
- 所有新版本必须能回滚到已验证包或原车桌面；不得删除原车 Launcher 或破坏现有恢复路径。



## 2026-09-14 用户最新迁移授权（覆盖旧的历史保留要求）

用户已明确要求删除中间版本、过时交接、实车截图、模拟器及缓存，并要求跳过备份校验直接删除。保留原始基线及别名、已验证V2、V3.11完整功能来源和R28当前代码、项目密钥和必要恢复资料。历史比较测试如引用已删除父版本，应调整到保留检查点，不为旧测试恢复全部历史版本。R28仍未完成全功能回引和实车验证，不能称为已验证全功能稳定版。

迁移入口为PROJECT_HANDOFF.md。安卓SDK/JDK/JADX不上传，换机使用已有环境。敏感项目资料仅进入加密迁移包，解密口令和GitHub登录凭据不上传。不得将未完成或失败的临时备份称为有效恢复路径。
