# 智UI Launcher 稳定性修复交接文档

交接日期：2026-09-09（北京时间）。工作区：`D:\apk`。目标：比亚迪海豚 2021 款时尚版，车机版本 `13.1.22.2409213.1`。

## 1. 当前结论与交接边界

**全部本地检查尚未完成，现在仍应继续本地工作，尚未到需要连接车机的阶段。** 当前成果是保留原版架构的分项稳定性修复检查点，完整 V2/V3 功能尚未修复回引，不能当作全功能稳定版安装交付。

用户确定的工作顺序是：先完成本地能够执行的代码审计、问题修复、测试与成品核验；明确剩余只能依赖真实 Android/OEM/车机验证的项目后，再连接车机检查和定向修复。本次用户要求编写交接文档，因此停在现有检查点整理证据，没有执行 R11 草稿，没有新增修复、构建、设备查询或安装。

状态应分成三层，不能混写：

| 层次 | 当前状态 | 接手处理 |
|---|---|---|
| R9b | 已完成分项代码、自动检查、人工审计记录及进度登记 | 作为记录完整的上一检查点保留；不是实车回滚包 |
| R10 | 已构建，自动检查已完成；人工审计与正式记录尚未收尾 | 先完成证据收尾，不能因 `checks_passed=true` 就宣布全计划完成 |
| R11 | 仅有补丁脚本草稿；未执行，未生成对应工作树或 APK，尚无专用测试 | 审查草稿后再决定实施，不能写成已修复 |

本轮本地候选均未实车验证，未进入 `D:\output\zhiui\clear`。已有 V2 和原车桌面恢复路径必须保留。

## 2. 接手优先阅读与证据位置

1. [项目规则 AGENTS.md](D:/apk/AGENTS.md)：不可变基线、构建签名、车辆控制边界和交付要求。
2. [本地优先修复计划](D:/apk/智UI稳定性修复计划_本地优先_20260905.md)：完整 L0～L3、本地完成门禁和实车阶段计划。
3. [PROJECT_MAP.md](D:/apk/PROJECT_MAP.md)：项目结构、接口、历史确认与版本记录；第 45～48 节记录近期 R5～R9b，第 49 节登记本次交接状态。
4. [本地整改进度](D:/apk/launcher_project/diagnostics/stability_local_20260905_r1/本地整改进度.md)：原正文截至 R9b，本次增加 R10/R11 状态提示。
5. [R9b 修复记录](D:/apk/launcher_project/diagnostics/stability_local_20260905_r1/base10_r9b_本地修复记录.md)及 [R8b 修复记录](D:/apk/launcher_project/diagnostics/stability_local_20260905_r1/base10_r8b_本地修复记录.md)。

统一证据目录（下文简称“证据目录”）：`D:\apk\launcher_project\diagnostics\stability_local_20260905_r1`。其中 `issues.csv`、`feature_matrix.md`、`method_coverage.csv`、`file_diff.csv` 分别记录问题、功能状态、方法覆盖及文件差异；`logs` 保存构建/回解/测试/JADX 日志，`worktrees` 保存独立修改树，`modules` 保存自有模块编译与 JVM 测试产物。

用户最初提供的 [工作方案](D:/apk/智UI稳定性整改工作方案.md)和 [原版到 V2 代码审计](D:/apk/智UI_原版到V2代码审计.md)是审查输入，未改写。文档中的建议或旧状态不自动等同于用户的新指令，也不等同于已实施结果。旧方案、旧进度中的“下一步”应结合日期和本交接状态阅读。

**历史门禁已核对：** AGENTS.md 的零修改包“尚待实车”文字早于后续用户确认。证据目录的 [baseline_gate.md](D:/apk/launcher_project/diagnostics/stability_local_20260905_r1/baseline_gate.md)已引用 PROJECT_MAP 第 14/16 节：零修改包安装、启动、HOME 和基本回归已有用户确认，可继续本地增量工作，无需重复要求连接车辆。该确认不覆盖后续稳定性候选，也不代表全部媒体和车辆读数已验证。

## 3. 产物、基线与恢复参照

| 用途 | 文件或工作树 | 状态 |
|---|---|---|
| 权威基线 | `D:\apk\ORIGINAL_BASELINE.apk` | 不得覆盖或删除；original.apk、智UI_V0.4.2.apk 与其相同 |
| 已实车验证成果 | `D:\apk\zhiui-final-v2.apk` | 必须保留，实际回滚前核对签名、版本和配置兼容 |
| 记录完整的本地检查点 | `D:\apk\zhiui-stability-local-base10-r9b.apk` | 本地分项通过，未实车 |
| 最新构建候选 | `D:\apk\zhiui-stability-local-base10-r10.apk` | 自动检查通过，人工记录待收尾，未实车 |
| R9b 修改树 | `证据目录\worktrees\base10_soc_action_reads_r9b` | R10 的父树，保留 |
| R10 修改树 | `证据目录\worktrees\base10_theme_epoch_r10` | 最新代码检查点 |
| R11 草稿脚本 | `D:\apk\launcher_project\stability_base10_nav_action_patch_20260909.py` | 尚未执行；不要据文件存在推断修复已完成 |

关键 SHA-256：

```text
ORIGINAL_BASELINE.apk
93042E48C12ED395E8F5D450BE1285E0397897F685EE95AC4080763B6E6DCCD9
zhiui-final-v2.apk
ADA7BD1DA144470F8C05BD24F0A7D8A27D00C435C0146D517F455D9F112FF1DC
zhiui-stability-local-base10-r9b.apk
B2FA96DEE07F796F2C77CBE8E9164DCAD8B339C6BD82D564BBA68B20AA5FD646
zhiui-stability-local-base10-r10.apk
BEA43A08CD26B7587D41853543505894EDB8C79559B5B54EBA571623D77995E0
```

R10 大小 30,080,319 字节；包名 `com.byd.launcher`，versionCode `1`、versionName `1.0`、minSdk `24`、targetSdk `34`、ABI `arm64-v8a`。开发证书 SHA-256：`C2BA875AFBF2EFBBFFF44CB64D4D5E16DD4CB1FEF56E441FC8C3BDE019FDB13C`。以上来自现有构建与验证记录；继续构建或安装前应核对所选文件身份。

累计检查覆盖 22 个保护 APK，R10 检查记录全部未变。所有历史包、keystore、日志、失败草稿、实车截图和正式交付物均保留。开发包不能凭同包名覆盖原证书包；不得自动卸载或清除数据。

## 4. 已完成的本地工作

下列“完成”仅指列明的分项范围及已有证据，不表示整个功能、所有调用方或车机运行已通过。

| 阶段 | 已处理的用户可见问题或稳定性风险 | 验证范围与仍有的限制 |
|---|---|---|
| 基础审计 | APK 身份与版本差异、JADX/apktool 交叉分析、问题/方法/功能清单、本地优先计划和旧门禁核对 | 已建立索引；完整方法、关联调用方和 API 人工判定尚未完成 |
| BASE-01～04 | 壁纸页面退出后的回调释放，Adapter 队列/线程关闭，视频缩略图 Retriever 异常释放，旧图片结果和复用控件误写防护 | 局部修复已有构建和测试；目录、轮播与 V2/V3 功能组合仍待处理 |
| BASE-05/07 | 壁纸后台任务限为 2 工作线程、4 等待任务；每 Adapter 至多 2 个待界面图片结果 | 防止任务和待显示位图无限积压；不代替真实内存压力测试 |
| BASE-06 | 拦截已核实的两个 policy_control 写入入口，设置只读并显示历史残留 | 主线不新增全局系统栏写入；可选系统策略另行评估 |
| BASE-08/09 | 状态栏创建失败回滚、控制器复用；快捷设置永久清理、释放失败保留引用以重试、60 个入口守卫 | 局部生命周期链已验证；V2/V3 状态栏完整回引仍未完成 |
| BASE-10 R1/R2 | 所属主线程队列在销毁后拒绝投递/执行；主题发布核对当前 Service 和观察到的偏好 | 主题查询代次与偏好往返问题由 R10 补充，尚待人工收尾 |
| BASE-10 R3/R4/R6 | 九处状态读取共用 2 工作线程、8 等待任务的池，按所有者取消排队任务；纠正导航轮询与仪表初始化类别冲突 | 不主动中断正在运行的 native/厂商调用；队列有界不代表查询耗时有界 |
| BASE-10 R5 | 导航读取加入查询标识、普通关闭失效及根视图身份检查；有效结果在主线程提交缓存与原显示回调 | 导航拖动/操作等待和待界面结果上限仍待 R11，不可提前算完成 |
| BASE-10 R7 | 仪表初始化、刷新、缓存预填及重试统一归属；旧结果不写新面板，拖动/关闭使旧读取失效；失败投递不占重试状态 | 保留原五级重试等待；生产 JVM 30,213 项断言及源码/成品分支检查；等待中断处理仍待复核 |
| BASE-10 R8/R8b | 磁贴首次/轮询/单项复查在有效时提交缓存；每磁贴 1 个待界面结果，后台池按磁贴身份区分；能量回馈/保电说明与新缓存一致 | 源码/成品 95 项场景；磁贴生产模块 20,024 项、池扩展 31,044 项 JVM 断言；保持原查询与操作请求 |
| BASE-10 R9/R9b | SOC 初始化/轮询共享有效性检查，关闭/拖动使旧结果失效；原异步操作等待期间暂停状态回写，旧回调不能解除新等待 | 源码/成品 122 项场景，ReadAfterAction 生产 JVM 50,012 项断言；15 秒为界面等待上限，不取消、重试或保证车辆命令完成 |

R9b 累计 92 个文件差异均有归属，构建、对齐、签名、aapt/apkanalyzer、最终 apktool 回解和精确内容核验通过。R8/R9 的数字存在继承关系，不能相加当作互不重复的全量测试数。

**测试边界：** JVM 测试运行自有生产逻辑和真实 JVM 线程；smali 测试运行受限指令解释器并替换 Android UI、时间、查询及部分模块。它们不等于 ART 验证、真实 WindowManager 行为、厂商/native 调用或实车结果。Android 29 声明初筛仍有 127 个基线候选且无新增；127 不是确认的缺陷数，也不表示已全部兼容。

## 5. 最新 R10：已完成什么、还欠什么

R10 在 R9b 基础上增加主题查询标识、每控制器至多 1 个待界面主题结果，以及进程内偏好写入代次。即使偏好从 A 改成 B 又改回 A，旧查询也不能因数值相同而重新有效。关闭/永久销毁使旧查询失效；原主题查询、模式计算、游标释放与 QuickSettingsThemeApply 保留。

五个增量文件：`ThemeRead.smali`、`ThemeDeliver.smali`、`smali/o/ff.smali`、`smali/o/ie.smali`、`smali/o/ci.smali`。均位于 R10 修改树。修复脚本为 `D:\apk\launcher_project\stability_base10_theme_epoch_patch_20260909.py`，测试脚本为 `D:\apk\launcher_project\test_base10_theme_epoch_smali_20260909.py`。

已核实的现有证据：

- `base10_r10_finalize_status.json`：`checks_passed=true`；ThemeRead 的 JADX 退出码 3，ThemeDeliver 为 0，均要求人工检查。
- `base10_r10_branch_tests_final.json`：源码/最终成品检查，22 项主题场景及继承 122 项，共 144 项；父版本恢复比对通过。
- `base10_r10_verification.json`：上述 APK 哈希、5 文件增量、无意外源码或最终业务差异。
- `base10_r10_cumulative_content.json`：累计 95 文件差异有归属，22 保护包保持，原资源声明和共享车辆分支检查通过。
- `base10_r10_cumulative_signature_check.json`：Android 29 候选仍 127，无新增。
- `logs/base10_r10_build.txt`、`logs/base10_r10_apkanalyzer.txt`：构建、签名、包信息与解析记录。
- `logs/base10_r10_jadx_0_debug.txt`：唯一报告的错误为既有 `NavBarService.Ε(ImageButton,String)` 的 RegionMakerVisitor 错误；不能将整个 JADX 过程称为零错误通过。

**接手首先完成 R10 人工收尾：** 对照最终 smali 复查 ThemeRead/ThemeDeliver、ci 偏好写入钩子、ie 构造与提交、ff 关闭入口的寄存器、异常路径和所有调用方；登记 JADX 错误及交叉确认结论；形成 R10 修复记录和人工审计文件，再更新进度、问题表、功能矩阵和 PROJECT_MAP。`base10_r10_draft_status.json` 尚保留早期草稿状态，应在完成收尾后更正，不能仅凭文件名忽略后续自动证据。

需要保留的设计边界：主题同步可发生在面板显示前，不能机械增加根视图检查；网格重建不应直接使已安排的合法主题读取失效。偏好代次仅覆盖当前进程中已核实的 setter，不声称能检测外部写入或未审查的动态键路径。

## 6. 未完成工作的执行计划

### P0：先收尾 R10，再审查和实施 R11

R10 按上一节完成，不因工具会话已结束而重建覆盖现有 APK。完成后审查 R11 草稿：目标是补齐导航音量拖动/操作期间的读取保护、严格面板身份以及每控制器 1 个待界面结果，保留原查询和操作请求。

R11 预计使用新树 `base10_nav_action_reads_r11` 和独立文件名。目前二者均不存在。草稿需先检查构造器 selector、实际跳转标签、寄存器和异常出口，补充拖动起止、用户/程序更新区分、旧查询、旧操作回调、关闭/重开、任务拒绝与异常场景，再按单项流程构建。尤其不能在 je 构造器尾部简单依据已被修改的 selector 参数判断导航分支；也不能把界面等待超时解释成命令超时或完成保证。

完成标准：专用源码/成品测试、生产模块必要并发测试、原导航 23 项及受影响继承回归、源码/成品精确差异、累计内容与保护包、API 初筛、签名/解析和 JADX-smali 人工确认齐备，所有记录与实际 APK 一致。

### P1：完成 BASE-10 剩余线程、等待和调用方审计

| 剩余项目 | 具体工作 | 完成标准 |
|---|---|---|
| 等待中断 | 审查 ff 静态查询及 ie selector28、je selector1 的 CountDownLatch/await 异常路径，处理吞掉中断、退出后继续回写等问题 | 超时、中断、销毁、正常完成均有明确行为和对应本地证据 |
| 背景玻璃任务 | 确认实际调用方、直接建线程、300ms 多轮等待、InterruptedException 处理和结果归属 | 队列/任务数量受限，关闭后无旧结果更新，取消/中断行为可验证 |
| 其他后台/操作线程 | 全量区分可替换状态读取与用户动作；检查未关闭资源、异常、等待和回调归属 | 不把用户动作当作可丢弃读任务；不因排队策略改变原操作次序 |
| 完整时序 | 覆盖同一控制器重复开关、旧预备根与新活动根、销毁后回调、多个入口同时触发 | 关联调用方、工厂和各 selector 均有审计结论，不能仅核对常量表 |

分类注意：ie 默认分支的 300ms 等待属于蓝牙设置回退，ee selector0 的 300ms 属于 Wi-Fi 路径；不要仅凭相同等待时间都归到背景玻璃任务。

### P2：逐项修复并回引 V2/V3 功能

当前基线修复分支尚不包含完整 V2/V3 功能。这部分是主要剩余工作，不能用“已恢复原版”代替用户要求的功能修复。沿用原架构，一次一个主要功能，每项单独产物、差异、测试与回滚记录。

| 计划项 | 要完成的本地工作与验收重点 |
|---|---|
| L01 状态栏/导航栏 | 复核 V2/V3 所有者、创建/清理和失败恢复；明确各窗口数量，避免重复挂载和失效反射 |
| L02 原车控/车辆信息布局 | 保留原布局 ID、类型、变体及全部使用方契约；新面板若必要使用独立资源 |
| L03 设置窗口 | 真正释放窗口并清空所有者字段，覆盖重复关闭、显示失败和销毁；不能只隐藏 |
| L04 壁纸目录 | 初始、刷新、主题切换使用一致策略；覆盖缺失、空目录、损坏/非图片及大图内存问题 |
| L05 壁纸轮播 | 关闭及 5/10/15 分钟设置只有一个有效调度；改间隔撤旧任务，关闭/不可见/销毁取消 |
| L06 模式键/广播 | 键码 289 的关闭、原功能、播放暂停、空调映射分别核验，跨入口去重；媒体窗口事件不误开空调，审查广播来源 |
| L07 空调面板 | 多入口单一窗口所有者，重复事件、dismiss、销毁和创建失败不残留；只测试 UI 与替身，不发送车辆控制请求 |
| L08 本地媒体 | 会话切换成对注销/注册；空标题/封面清理，旧会话回调不覆盖新会话 |
| L09 歌词调度 | 500ms 刷新唯一，start/update/stop/reset 清晰；暂停、不可见、销毁停止，无周期反射 Activity |
| L10 网络歌词 | 独立且默认关闭的分支；1 工作线程、有界最新请求，取消并断开连接，流必关闭，超时/大小/缓存/重试有限，防 A→B→A 旧响应 |
| L11 系统栏全局策略 | 保留主线已核实写入拦截与残留提示；可选写入功能不与主线混合，不在本轮擅自恢复全局写操作 |
| L12+ HOME/应用入口 | 缺失应用、无启动入口、返回 HOME、歌词点击、导航应用配置与必要布局兼容均有失败处理 |

### P3：补齐完整人工覆盖与总体本地门禁

1. 更新方法/文件/功能/问题清单，每个已改方法及关联调用方都有判定；逐个分类 127 个 Android 29 声明候选，核对 SDK guard、工厂、继承链、访问权限、寄存器和异常路径。声明扫描不能替代这些检查。
2. 对照历史 V2/V3 中间版本与日志，明确每个功能是已回引修复、恢复基线、尚未引入或有依据地留待实车，不能只比首尾 APK。
3. 验证 V2/V3 配置升级与回滚，避免以清数据掩盖问题；做状态栏+壁纸、设置+轮播、面板+销毁、媒体+歌词取消等组合场景。
4. 对最终候选完成构建、对齐、签名、aapt/apkanalyzer、最终回解、资源契约和累计差异检查，确认无非预期 native/assets/共享分支改动、测试替身未打包、保护包哈希未变。
5. 明确列出仍不能本地运行的 ART、OEM、窗口、媒体权限及厂商接口场景，以及必须实车验证的原因、最小步骤、预期结果、日志和恢复方式。

**进入车机阶段的条件：** 已知且本地可定位的问题已修复或明确处置；L01～L12+ 状态齐全；相关测试和最终产物门禁完成；剩余项确实依赖真实系统/硬件。当前不满足这些条件。已有浏览器模拟页面不能作为 APK 或车机运行证明。

### P4：通过本地门禁后，再连接车机

1. 由用户确认本次目标设备和安全停车状态；不复用旧 IP 或未经核对的序列号。先审核 `D:\apk\launcher_project\capture_stability_baseline.ps1`，明确设备、采集时长和独立输出目录。
2. 先做只读基线：车机固件/API/ABI、当前包/签名/版本、HOME、进程、窗口/服务/媒体/内存及定向 logcat；保留原日志，不隐式安装、root、清日志或执行控制命令。
3. 核对具体候选、签名、配置迁移和恢复路径后再安装。最小冒烟从启动、HOME、应用列表、设置开关和只读显示开始；不把 SOC、仪表等原有命令链的本地替身测试转成实车控制试验。
4. 按功能矩阵检查重复窗口、设置、媒体切换和按键映射，再采集 0/15/30/60 分钟同条件资源数据，随后按计划长时间观察。区分系统回收、应用崩溃、ANR、窗口泄漏和厂商调用失败。
5. 失败先保留表现、截图和定向日志，返回原车桌面；核对兼容后恢复已验证 V2。回到本地修复对应问题并复测同一场景，不连续叠加无证据补丁。
6. 实车通过后再更新最终报告并进入正式交付目录；任何卸载仍须用户明确确认，不删除原车 Launcher。

## 7. 构建与测试操作注意事项

- 脚本主目录：`D:\apk\launcher_project`；自有模块源码：`D:\apk\launcher_project\stability_modules\src\com\byd\launcher\stability`。稳定性构建脚本为 `stability_build_20260905.ps1`，参数包括 `-DecodedDir`、`-OutputName`、`-LogPath`；先阅读再调用，独立新名称，禁止覆盖已有候选。
- 固定工具：Python `C:\Program Files\Python312\python.exe`；Java/Javac `D:\Android\.jdk21\bin`；apktool `D:\apk\apktool_3.0.2.jar`；框架目录 `D:\apk\apktool-framework`；JADX `D:\Android\apk_analysis_dd\jadx\lib\jadx-1.5.5-all.jar`；SDK build-tools `C:\Users\L\AppData\Local\Android\Sdk\build-tools\35.0.0`。不要依赖 PATH。
- `stability_finalize_candidate_20260909.py` 执行成品回解、分支测试、精确比对、累计内容/API、apkanalyzer 和 JADX。R10 这些自动步骤已经结束，无需为了接续会话再次运行。若以后确需恢复中断任务，先检查已有产物及脚本的 `--resume` 前提，保留原失败日志。
- `stability_verify_base05_cumulative_20260906.py` 已显式接入到 R10；新 R11 必须补充自己的归属清单，不能放宽成忽略未知差异。共享混淆类要检查未改 selector，不能整类豁免。
- 修改树通过 apktool 建立；原包存在仅大小写不同的资源，不能用普通 ZIP 解压创建 Windows 修改树。复制树只排除根部构建产物，不误删 `unknown/META-INF/com/android/build` 等原内容。
- apktool 会重命名标签；成品测试应依据实际跳转目标，不能依赖源码标签文字。补丁插入前区分真实标签定义与 packed-switch/sparse-switch 载荷，避免重复替换破坏结构。
- JADX 可能在既有 Service 方法报错或错误呈现匿名类/分支；新代码必须回到最终 smali 确认，不把反编译 Java 当原源码。已有失败草稿和日志是问题证据，保留即可。
- `stability_record_tiles_20260909.py` 和 `stability_record_soc_20260909.py` 已执行，带归档保护；不要重复运行覆盖记录。新增 R10/R11 记录使用相应新名称。
- 文档不记录密钥密码；不要整份输出可能包含签名敏感信息的历史项目日志。沿用本地构建脚本的密钥处理，不复制凭据到交接内容。

## 8. 接手后的第一份交付

先交付 R10 人工审计和记录收尾，再交付经审查、测试并构建的导航 R11 分项；随后继续 P1～P3。每次汇报写明“修了什么、验证到哪里、还剩什么”，保持“本地未全部完成、未实车”的准确状态，直到总体门禁确实满足。

本交接以现存文件与自动验证记录为依据；没有把未执行脚本、旧计划文字、构建成功或本地替身测试计入实车完成项。
