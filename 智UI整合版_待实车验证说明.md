# 智UI整合版：待实车验证

## 交付状态

- 状态：**已完成静态构建与签名校验，待实车验证**。
- 待测 APK：`D:\apk\zhiui-integrated-pending-vehicle.apk`
- SHA-256：`49502F728BC30BD5AFEB87B452B3737A268565E531C93BD9C377AEB617E105A4`
- 不得在实车验证通过前移动到 `D:\output\zhiui\clear`。

## 已整合内容

1. 系统栏透明化、动态壁纸延伸、导航栏透明背景与导航悬浮层方案。
2. 状态栏动态更新及高德地图相关适配。
3. Wi-Fi/壁纸定时刷新逻辑（5 分钟）。
4. 方控模式键新增“空调控制”（`ac_popup`）映射：
   - 仅在模式键的设置下拉项中显示；
   - 仅处理首次 `ACTION_DOWN`，忽略长按重复事件；
   - 通过现有车辆控制 Overlay 打开/关闭空调控制界面；
   - 复用现有 `getAcState`、空调开关、温度、风量、循环、前后除霜 IPC 链路；不引入 `dd.apk` 协议。

## 已完成的静态检查

- apktool 编译成功；
- 已清除反编译资源的重复定义，aapt2 资源链接成功；
- APK 已 zipalign；
- `apksigner verify --verbose` 通过：v2、v3 签名均为 `true`；
- 包使用本地测试签名 `CN=ZhiUI`，不等同于原厂量产签名。

## 实车验证清单

### 启动与系统栏

- 冷启动、强制停止后启动、连续重启；
- 主界面、动态壁纸、状态栏和导航栏无黑底、无重复栏；
- 原生 `NavigationBar0` 覆盖关系与导航点击正常；
- “所有应用”打开和返回主界面正常；
- 高德地图前后台切换、横竖屏/全屏场景下状态栏正常；
- Wi-Fi 状态变化和连续运行超过 5 分钟后壁纸刷新正常、无卡顿。

### 空调控制

- 在方控设置中，仅模式键可以选择“空调控制”；其他按键不显示该选项；
- 模式键设置为“空调控制”后，单击能打开/关闭控制界面；长按或连按不重复打开；
- 打开后读取真实 `getAcState`，并验证空调开关、温度、风量、循环、前/后除霜；
- 验证空调数值边界、指令失败/服务断开后的 UI 状态；
- 在桌面、地图、倒车、相机和其他全屏应用中测试显示与关闭；
- 熄屏/亮屏、服务重连、返回键和异常窗口清理后，无残留 Overlay。

## 已知待确认项

- 当前空调入口复用已有车辆控制 Overlay 与真实 IPC；最终实车需确认它在目标车型上呈现的控制项、状态回写和交互范围均符合单区空调需求。
- `NavigationBar0` 仍由智UI悬浮层覆盖；车机系统升级后需重新验证层级和触控。
- `CoreService` 的 `resource hotfix not found!` 既有提示未在本轮静态检查中处理。
