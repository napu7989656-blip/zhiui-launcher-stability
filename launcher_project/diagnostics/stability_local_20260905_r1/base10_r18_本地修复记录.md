# BASE-10 R18 通知监听初始化（2026-09-11）

APK `D:/apk/zhiui-stability-local-base10-r18.apk`，SHA-256 `AEB9E24B4C4FE7F749BBE8F1488AAF495176CEE91CC20A4EC91F6BB62CD13778`，30,080,319字节。11增量/130累计文件；父R17和历史包保留。未实车，完整计划未完成。

本分项解决已复现的命令空字符串“假成功”：以系统设置读回的实际组件状态确认授权，读取异常才使用有界备用进程；备用进程正常完成且退出码0才认作有效读取。未知、畸形列表不写回；组件精确匹配并支持相对类名，合并时保留最新读取中的其他组件，拒绝异常输出和超限列表。已授权时不再先撤销再重授。原授权与备用设置操作目的保留，shell列表值加引号防止类名中的$被展开。

备用进程不再先readLine阻塞再计时；限制等待与输出，区分完成/失败/超时，响应归属失效与中断，finally请求终止进程并关闭流。原四个媒体刷新时点保留，投递与执行核对捕获Service身份。主流程仍采用原APK架构，没有重写Launcher。

验证：源码/成品各316组（14桥接+302继承）；生产JVM进程66、列表47、流程51项断言，含真实测试子进程。Java8/D8 min24通过，生产class与测试class逐字节相同，DEX不含测试替身。构建、对齐、开发签名、解析、独立回解、精确父恢复、130累计文件及22保护APK检查通过，Android29候选127且无新增。数字不代表Android/ART或实车场景全覆盖。

JADX Host和NavBarService退出3均仅报告既有Ε方法RegionMaker错误，其他四类退出0。InitProcess的Java输出把计时与ProcessBuilder.start展示顺序改错并有finally类型推断瑕疵；最终smali确认计时在start前，生产JVM与最终指令用于判断行为。手工细节见base10_r18_manual_audit.json。

边界：进程创建、平台流和清理耗时仍需设备观察，destroy不保证瞬间终止且不撤销已经执行的命令。读取/合并/写回不是跨应用原子操作，不能保证读取后其他应用并发修改不会竞争。系统设置读取能力需目标车机验证。仍须完成同Service重复初始化、共享ADB层中断/重连、输入法和其余m9调用链，再完成其余P1、完整V2/V3回引和P3总体门禁。

未来本地门禁完成后由用户停车验证：在现有配置下检查授权状态与媒体会话恢复、重复HOME/Service重连；采集NavBarSvc、通知监听、媒体会话与crash日志并核对其他监听器仍在。失败先回原车桌面保留日志，再核对签名版本配置后恢复已验证V2。当前不连接设备、不执行授权命令、不卸载、不进入正式交付目录。
