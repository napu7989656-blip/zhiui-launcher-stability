> 2026-09-12更正：发现R20将Application Context误判为Service，导致实际媒体准入失败。此前测试替身不准确；本记录的历史通过不能作为R20功能正确证明。R20包保留作诊断，不应安装使用。修正与真实构造回归已完成R20b成品分项核验，详见base10_r20b_本地修复记录.md。

# BASE-10 R20 媒体会话回调隔离（2026-09-11）

本版本分项本地检查完成，整体计划未完成，未实车，不进入正式交付目录。

APK：D:/apk/zhiui-stability-local-base10-r20.apk，SHA-256 `C4B11547526B3E00107A4B9488E97B0D2A71A7955DB2B118913FA46F9C7AA13C`，30,080,319字节。父版本R19保留；工作树base10_media_session_r20。

## 改动与用户可见行为

每次媒体会话绑定创建独立回调，解除时先清当前引用并更换恢复令牌，再注销捕获的旧回调。元数据、播放状态和会话销毁事件均校验当前管理器、Service与回调身份，防止旧事件清空新会话界面。新会话绑定后清800ms标题去重状态，允许同名歌曲的新会话更新。注册异常解除刚创建的实例，后续可重试。原500ms恢复扫描保留，但再次解除、已绑定会话或管理器/Service替换会使旧恢复任务失效。

只改a9、y8与td selector16三文件；td其他分支、播放控制、URI加载与封面任务未改。精确逆补丁可恢复R19源文件，累积133文件全部纳入既有白名单检查。

## 验证

源码与独立成品回解各343场景组（本项17＋既有326）通过。覆盖旧/空/替换回调、管理器/Service销毁或替换、重复销毁、延迟恢复失效、注销异常与注册异常回滚、同名标题去重重置。Android调用使用替身，异常处理器在确认实际捕获范围后单独执行，不声称ART运行通过。新增方法声明缩进及测试对回解标签重命名的依赖已修正，首次工作树保留为base10_media_session_r20_draft1_method_indent；失败验证日志保留。

构建、zipalign、开发证书签名、aapt/apkanalyzer、精确源码/成品比对及累计检查通过；22保护APK不变，Android29签名候选127项无新增，既有API人工判定仍开放。

JADX y8退出0；a9/td退出3仅报告既有NavBarService.Ε RegionMaker错误。a9新注册try范围被伪源码展宽，base10_r20_register_catch_proof.json证明实际成品仅捕获registerCallback一条调用。td默认省略run，独立partial退出1，case16准确呈现新恢复守卫。以成品smali和实际分支测试为准。

## 边界、下一步与回滚

框架注销异常后可能仍持有旧监听，逻辑拒绝不等于物理释放。旧封面后台任务仍可能覆盖新结果，图片流/连接异常清理、媒体池有界与关闭、重复管理器、全链路图片归属仍待继续。补充审计见媒体读取与会话补充审计_20260911.md。P1/P2/P3及完整V2/V3回引未完成。

本地源回滚使用保留R19工作树/逆补丁；R19/R20均未实车，不作为已验证实车回滚包。已验证zhiui-final-v2.apk及原车桌面恢复路径保持不变。此轮无网络请求、设备连接、安装、卸载或车辆操作。
