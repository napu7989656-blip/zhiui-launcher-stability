# 本地整改进度（2026-09-08，BASE-10 r4）

**全部本地检查尚未完成；未连接车机，仍不满足进入实车阶段的条件。** 当前包是原版架构上的分项修复检查点，尚未完成V2/V3功能修复整合，不能作为全功能稳定版交付。

## 当前检查点

- APK：`D:/apk/zhiui-stability-local-base10-r4.apk`
- SHA-256：`10E02F54A657144210CF7B85D3DD074D55A4A19DF5882C2C838A6AB6999AB404`
- 父版本：BASE-10 r3，包含此前BASE-01 r2、02、03、04、05、07、06、08、09 r3、10 r1/r2修复。
- 包名com.byd.launcher，versionCode 1；minSdk24、targetSdk34、arm64-v8a；与V2相同开发证书，V2/V3验签通过。
- 成品构建、zipalign、aapt/apkanalyzer、最终回解和精确代码差异通过。累计62个文件变化可归属，原资源声明保留，22份保护APK哈希不变；Android29声明初筛127条候选无新增，尚未全部人工判定。

## 已完成的本地分项修复

| 范围 | 已有结果 |
|---|---|
| BASE-01～04 | 清理壁纸页面回调、解除Adapter/关线程池、异常释放视频Retriever、拒绝过期壁纸结果与复用控件误写 |
| BASE-05/07 | 壁纸解码2线程/4等待；每Adapter至多2个待UI结果，避免无界积压 |
| BASE-06 | 核实的全局policy_control写入在两个执行入口拦截；设置只读并显示系统残留 |
| BASE-08 | 状态栏添加失败回滚、失败资源引用保留、避免重试叠窗、控制器复用 |
| BASE-09 r3 | 快捷设置永久销毁、监听/窗口/动画/队列清理及晚到入口守卫 |
| BASE-10 r1/r2 | 所属主线程队列拒绝销毁后消息；主题结果校验Service身份及观察到的偏好后发布 |
| BASE-10 r3/r4 | 九处仪表亮度、导航音量、SOC目标、主题、磁贴读取进入共用2线程/8等待池；按所有者/类别替换等待项，退出取消所属等待项 |

上述补丁各有源码/成品验证记录。线程池已执行真实JVM 10,000次提交和并发/取消/异常用例；当前成品九处提交参数、主题19项、队列6项、60入口/15清理分支通过。JADX旧服务方法存在已定位的反编译错误，精确行为以成品smali对照为准。

## 仍需完成的本地工作

1. **BASE-10未全部完成。** 防止同类别并行读取的旧结果覆盖新结果，补齐面板打开代次、查询代次和偏好A→B→A的识别；处理背景玻璃线程/等待中断，按操作语义审计其余用户操作线程和调用方。线程/队列上限不是任务耗时保证，已运行native请求未被强制取消。
2. **V2/V3功能修复及回引。** 状态栏所有权、原布局契约、设置窗口释放、壁纸目录/轮播、模式键/空调面板、媒体空值与会话、歌词生命周期/有界网络、HOME和点击兼容仍待逐项完成。
3. **完整人工代码覆盖。** 修改方法及关联调用方尚未全覆盖；Android29的127项声明候选仍需沿guard/factory/继承链复核，不能当成127个已确认缺陷或全部兼容性通过。
4. **整体本地门禁。** 功能组合回归、配置升级/回滚兼容、最终实车最小步骤/日志与失败恢复清单尚未闭环。

## 证据与边界

本目录 `issues.csv`、`feature_matrix.md`、`base10_r4_本地修复记录.md` 和各版本JSON保存详细证据；历史进度归档于 `本地整改进度_归档_截至base10_r3.md`，版本沿革见 `D:/apk/PROJECT_MAP.md`。完整顺序以 `D:/apk/智UI稳定性修复计划_本地优先_20260905.md` 为准。

JVM执行自有生产代码及真实线程，但Android/生命周期边界用替身；受限smali用例执行实际指令分支，均不等于ART、真实窗口、权限、OEM实现、native或实车验证。全部本地可做项完成后才进入用户安全停车的实车检查。原包、已验证V2和原车桌面恢复路径保留；没有候选进入正式交付目录。


### BASE-10 过期结果与缓存写入的本地复现（2026-09-08）

`stability_audit_read_freshness_20260908.py` 已在 BASE-10 r4 源码和最终回解分别执行实际 Handler/lifetime、Lo/be 显示回调和 Lo/je 缓存写入指令。人工安排新值9先返回、旧值3后返回，当前显示与导航音量缓存都会回到3；对同一控制器模拟普通关闭/重开标志后，旧消息仍被接收。证据 `base10_freshness_audit.json`。这是受限指令执行加模拟返回顺序/框架调用，不是实际并发车机测试或完整开关窗运行；没有执行查询。

下一补丁不能只在Handler丢弃过期消息：仪表亮度/导航音量worker在投递前会更新控制器缓存，磁贴getter也会更新本地状态缓存。需要同时校验查询/面板代次并处理缓存提交，审计仪表重试计数/标志及getter调用方，避免旧缓存被随后UI或操作再次使用。“状态读取”表示没有新增车辆写请求，不等于getter没有本地缓存副作用。

当前APK仍是 BASE-10 r4；这个已登记的剩余问题尚未修复，不将本次复现记成稳定性通过或进入实车阶段。
