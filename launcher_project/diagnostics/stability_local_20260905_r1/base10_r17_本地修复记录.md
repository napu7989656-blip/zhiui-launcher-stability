# BASE-10 R17 Service初始化退出路径（2026-09-11）

APK `D:/apk/zhiui-stability-local-base10-r17.apk`，SHA-256 `A0371FB1BA3D0F2D0DE56F44C2018E4E277B37942D126E59D03D02D96E65CF48`，30,080,319字节。父R16b及全部历史包保留；1增量/120累计文件，未实车。完整本地计划未完成。

修复ea.run selector0：原Service失效、被替换或线程中断时，在13个关键检查点退出；等待抛InterruptedException时恢复中断标志并立即返回，避免shutdownNow后继续重试。原10次尝试、等待间隔、命令内容、policy拦截与其他selector保持。已经进入的ADB或嵌套初始化不会被这些检查点强制取消，不能视为总耗时上限或整条链已修完。

源码/最终成品各302组场景（9组初始化检查及293继承），包括6状态下全部13实际守卫片段和实际中断处理；不是完整Android初始化执行。构建、对齐、开发签名、解析、独立回解、精确逆补丁、120累计文件归属及22保护APK通过，Android29声明候选127且无新增。

成品核验器增加goto宽度等价处理，并修正packed-switch表项被误认作相邻标签的漏检；5项跳转宽度与6项switch反例/等价检查通过。旧检查器和失败日志均归档，已用新检查器重跑成品及累计核验，未修改APK。JADX默认退出0但run为未反编译占位；部分反编译把一处守卫条件反转、重试流程重建错误，已对照最终smali和实际分支测试，不把Java伪源码当正确实现。

仍待处理：NavBarService.Λ输入法初始化内部、Κ通知监听授权内部、m9 selector0/8/9及其他延迟回调的实例替换/销毁时序、onServiceConnected重复初始化和线程池释放。m9现有守卫只判断捕获Service已销毁，不检查当前实例；本轮没有将它改成全局取消所有用户动作。其余P1清单、完整V2/V3功能回引、API127人工判定、配置升级回滚与组合门禁继续开放。

未来本地门禁后由用户安全停车验证：正常HOME启动和Service重新连接后检查导航栏、输入法/媒体初始化是否正常，采集NavBarSvc/initCmdTools/crash日志；失败先返回原车桌面保留证据，核对签名版本配置后恢复已验证V2。不在车辆上故意制造故障，不主动执行车辆控制。当前无需连接设备，未验证候选不进入正式交付目录。
