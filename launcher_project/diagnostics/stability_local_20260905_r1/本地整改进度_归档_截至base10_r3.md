# 本地整改进度（2026-09-08）

当前仍处于本地审计与分项修复阶段。尚未满足完整本地门禁，未连接车机；这些候选不能作为 V2/V3 全功能稳定版安装交付。

## 已有具体证据的进展

| 检查点 | 用户影响与处理 | 本地证据 |
|---|---|---|
| BASE-01 r2 | 销毁桌面后释放属于该页面的全局壁纸回调，避免旧引用和两次读回调的空值竞态 | 六项源码/最终包分支用例、最终包差异；r1 复制缺失元数据已单独保留失败记录 |
| BASE-02 r1 | 销毁时解除壁纸 Adapter，调用已有线程池关闭并取消 Handler 队列 | 有/无页面回调、重复销毁、前序回归、最终差异 |
| BASE-03 r1 | 视频封面提取在成功、失败和异常退出时释放已构造的 Retriever | 七项故障分支用例；最终回解及其他共享 selector 不变 |
| BASE-04 r1 | 图片/视频旧结果不能更新已销毁页面或复用后的控件；主线程统一发布缓存/缩略图 | 源码/最终包各十四项结果用例、销毁/空数据守卫、前序回归；JADX/smali 核对 |
| BASE-05 r1 | 快速滑壁纸时，两个解码线程最多等待四个最新任务，不积压无限旧任务 | 自有 Java 实际 JVM 10,000 次提交、关停/中断测试；源码→D8→smali→最终 APK 验证 |
| BASE-07 r1 | 主线程暂停时，每个解码线程最多持有一个待显示结果，销毁时可中断等待 | 自有 Java + 真实线程，Handler/Looper 替身验证暂停/恢复、销毁清队列竞态、投递拒绝、中断、异常回调；源码/最终包结果回归及累计最终核验通过 |

BASE-06 r1 已完成已知全局策略写入的主线隔离：双执行入口守卫、只读设置与残留检测及最终包核验通过。编号 07 先完成，06 后完成。

BASE-08 r1 本地验证通过：状态栏/手势层清理失败保留引用并阻止重建，监听逐项注销，控制器复用；44 项 Java 断言和源码/最终各 20 项实际 smali 分支通过。

## 当前候选

- `D:/apk/zhiui-stability-local-base10-r2.apk`
- SHA-256：`EF61C11436EDB43E4AF914D041016B3985453EFC6A0B1B94D9726A73AD3C03E8`
- 父版本：BASE-10 r1；继承 BASE-01 r2、02、03、04、05、07、06、08、09 r3。
- 包名 `com.byd.launcher`、versionCode 1、minSdk 24、targetSdk 34、arm64-v8a；使用已有 V2 开发证书，V2/V3 签名与 zipalign 通过。
- 全量重建、最终回解、JADX 交叉核对和 apkanalyzer 已完成（JADX 服务类继承一处旧反编译错误，详见本次记录）；本次 2 项文件变化均可归属。它仍是开发检查点，不能当作已验证回滚包。
- 累计 59 个文件变化均已核对；原资源声明与共享类的其他 selector 完整保留，22 份保护 APK 哈希未变。Android 29 声明级扫描与原版一样为 127 条待分类候选，无新增签名告警；不能将此扩写为全部 API/ART 已验证。
- 回滚参照仍保留已验证的 `zhiui-final-v2.apk` 与原车桌面。真正安装前须重新核对当次设备、签名、版本与数据兼容；本轮没有安装操作。

## 新确认且必须继续处理的事项

1. **BASE-10 后台工作与晚到投递继续修复。** BASE-09 r3 已完成永久销毁、60 个入口守卫和布局监听所有权；BASE-10 r1 已用所属 Handler 拦截销毁后的晚到 UI 投递；r2 已把主题查询发布转回所属主线程并核对 Service 身份；并发上限/打开代次/中断、同一服务内偏好 A→B→A 和其他后台调用方仍需处理。
2. 原版 Android 29 声明初筛的 127 项候选并非 127 个缺陷。37 个平台 API 包装方法已建立 41 个直接调用方法索引；其中 MainActivity 窗口与 PiP Surface 两项 SDK>=30 守卫已逐指令复核，其他库的守卫/工厂链、继承构造器模式继续审计。
3. V2/V3 的状态栏所有权、原布局契约、设置窗口释放、壁纸目录/轮播、模式键/空调面板、媒体空值与会话生命周期、歌词调度与有界网络、HOME/点击兼容仍需按原计划逐项修复回引。当前恢复原版架构不等于这些功能已完成。
4. 全部修改方法/关联调用方的人工覆盖、本地组合回归、配置升级/回滚兼容、最终车机清单及只读采证脚本门禁仍未闭环。

## 验证边界

受限 smali 用例执行实际指令分支，但不等于 Android ART。JVM 测试执行新模块源码、真实线程及 CountDownLatch，Android 队列使用隔离替身，不能证明厂商 native 解码会及时中断。真实窗口、视频、权限、OEM 回收、播放器和长期内存留到全部本地可做项完成后再上车验证。浏览器模拟台未被当作 APK 运行证据。

详细证据为本目录的 `issues.csv`、`feature_matrix.md`、各检查点 `*_verification.json` / `*_branch_tests*.json`、`modules/*/jvm_tests.txt` 和 `logs/`。完整执行顺序仍以 `D:/apk/智UI稳定性修复计划_本地优先_20260905.md` 为准。

### BASE-09 后续审计证据（2026-09-07）

`stability_audit_quicksettings_lifecycle_20260907.py` 在 BASE-08 源码和最终回解分别执行实际 Lo/ie selector 4 → ff.ͻ() → selector 0 分支：即使 ff.ͺ/ͷ 已为 false、旧面板与预热 View 为空，仍会重新投递并到达 ContextThemeWrapper/布局创建边界。未运行 Android 构造、inflate、车辆命令或完整 onDestroy，也没有实际等待 500ms；延迟值来自原 close 方法指令。证据 `base09_lifecycle_audit.json`。

已索引 31 个直接持有或经 Object 转换为 ff 的候选类；这不是 31 个确认缺陷，也不代表全部方法需要修改。下一步检查永久销毁与普通关闭分离、两条 Handler 队列、预热/已打开/嵌入根 View、动画与监听注销、后台工作晚到结果和共享 selector，完成后再选定小范围补丁。BASE-08 不包含该后续修复。



BASE-09 r3 最终结果：43 项 JVM 断言、源码和成品各 60 个入口及 15 项清理/监听分支通过，前序回归、签名、全量差异和保护 APK 核对通过。详细见 `base09_r3_本地修复记录.md`。上面的 2026-09-07 审计段落为历史发现记录，永久销毁已在本次完成。


BASE-10 r1 已完成晚到队列修复的本地成品核验：25项JVM断言、源码和成品各6项队列分支及前一批60守卫/15清理分支通过。线程限流、代次、主题后台刷新仍待后续补丁，见 `base10_r1_本地修复记录.md`。


BASE-10 r2 当前成品已完成本地验证：主题发布19项分支及继承队列/清理用例通过；两文件增量、59累计文件、签名和22保护APK核验通过。历史段落中“主题后台直接刷新待修”已由r2覆盖此特定调用路径，线程限流和代次仍待修。详见 `base10_r2_本地修复记录.md`。


### BASE-10 后续只读线程池模块（2026-09-08，尚未接入 APK）

新增可维护 `QuickSettingsReadExecutor.java`，Java8/Android29 编译、D8 min24、仅三项自有类的 apktool 回解和 JVM 10,000 次提交/并发、队列替换、所有者取消、销毁、异常及拒绝测试通过。设计为进程内共用2个工作线程、8个等待读取；同一控制器同一类别只替换仍在队列的旧读取，取消时只移除该所有者等待项。已运行任务最多受全局2线程限制，取消等待项不等于中断native或已发送请求。

这是独立模块验证结果，尚未改动当前 BASE-10 r2 APK，也不能据此宣布 Launcher 所有读取已限流。下一步逐个核对读取 producer/selector，接入取消和实际调用点，再完成成品校验；用户操作分支不能按过期状态读取统一丢弃，面板/查询代次仍待修。当前本地检查点仍是 BASE-10 r2，证据 `base10_read_executor_module.json` 与 `modules/quick_read_executor_r1/`。未连接车机。


## 43. BASE-10 r3 七处读取接入有界线程池（2026-09-08，未实车）

当前检查点 `D:/apk/zhiui-stability-local-base10-r3.apk`，SHA-256 `A88184A1659BE4FD5191FFEA8012EEB88BF7EAF4EA314A5E2350CE2FD61BAA30`，父版本 BASE-10 r2。独立模块已正式接入七处读取：仪表亮度刷新/初始化、导航音量初始化/轮询、SOC目标初始化/刷新和系统主题查询。运行中的这些读取进程内共用2个线程，等待队列上限8；同一所有者/类别替换仍在等待的旧读取，满时丢弃最早等待的读取。控制器先标记停止，再清除属于自己的等待项；不会打断已发送请求或强制中断native。

已有 Java 模块的10,000次提交、所有者隔离、销毁/队列替换/异常用例通过。源码与成品实际指令测试确认七个原Runnable及参数保持，提交key/所属控制器正确，停止后取消与重复销毁有效，继承主题19项、队列6项和60入口/15清理分支通过。测试替身不进入DEX，不是ART。第一版接线测试脚本误将三值fixture拆成两值，修正测试解包后通过，产品代码未因此修改。

五个允许文件变化（ff、ie及3个编译模块类），累计62文件均可归属；原查询方法、用户操作和其他selector完整保留。全量构建、zipalign、同证书V2/V3签名、aapt/apkanalyzer、最终回解/JADX模块核对通过；22保护APK哈希未变，Android29声明级127候选无新增。证据 `base10_r3_verification.json`、`base10_r3_branch_tests_final.json`、`base10_r3_manual_audit.json`、`base10_r3_cumulative_content.json`、`base10_r3_cumulative_signature_check.json`。

BASE-10仍未全部完成：磁贴刷新两处后台producer已完成getter分类（`base10_tile_read_audit.json`），准备接入；背景玻璃处理、面板/查询代次、A-B-A偏好变化和中断仍待修。之前“线程池模块尚未接入”是r3以前的历史状态，不适用于上述七处。未连接车机，当前不作为V2/V3全功能交付包。
