# 媒体图片读取与会话绑定补充审计（2026-09-11）

基于R19实际smali，补充前一轮未完整展开的URI读取与三处内部元数据入口。R20正在单独验证会话回调隔离，本记录不是封面任务已修复的声明。

## 图片读取

a9.ʹ(String)先尝试ContentResolver.openInputStream并decodeStream，再针对http/https尝试HttpURLConnection，最后file://可回退decodeFile。ContentResolver流只在正常decode返回后关闭；HTTP流关闭与disconnect也仅在正常路径执行，decode/getInputStream/close异常均可能跳过后续清理。HTTP有连接和单次读取各2000ms超时，但没有总下载时限、响应字节上限或解码尺寸上限；inSampleSize=2不是像素上限。ContentResolver调用耗时仍由外部提供者决定。

HTTP成功结果写Ώ/ΐ缓存，同URL返回缓存原对象；URI函数并非总是交出独占图片。结合ͻ可能返回输入原图，后续丢弃任务不能无条件recycle。下一封面管线至少要统一结果代次、缓存归属、有效主线程提交，并明确URI缓存与借用图的处理。此次仅静态审计，没有发起网络请求。

## 会话入口

ͱ()获取当前metadata并主动清空标题去重/时间后调用Ͱ；ͷ()轮询每第三次获取当前metadata再调用Ͱ，当前controller可能在persistent-null路径被解除，因此原有再次判空应保留。Ͷ(List)以用户指定包/播放状态挑选会话，token相同直接返回；替换时先ˏ解除，再赋controller、注册回调、处理metadata与播放状态。原替换未清空标题去重，相同标题/歌手/封面存在性在800ms内可能抑制新会话更新。

原y8被所有绑定复用，只检查manager destroyed/有UI callback，不校验来源会话。onSessionDestroyed会解除当时的当前controller并清UI，之后延迟500ms通过td selector16重新扫描。R20专门为这些入口与延迟恢复加入实例/代次隔离，每次绑定创建新回调；不把这一项视作媒体完整生命周期或封面异步链已完成。

## 尚未关闭的边界

框架注销抛异常后旧回调仍可能被外部持有；逻辑拒绝旧事件不等于保证框架已释放监听。新回调隔离不能停止已经运行的封面读取，不能消除媒体池未关闭或重连重复manager。旧管理器销毁、活动会话监听、所有后台来源与直接小图分支仍需继续集成。所有结论限定为本地证据。
