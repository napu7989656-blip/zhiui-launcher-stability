# 媒体封面管线模块准备（2026-09-12，未接入APK）

新增MediaArtworkPipeline，建立三类后台来源和直接小图统一请求的生产Java流程：输入只包含本次metadata应提供的图片、URI及description备用图片/URI，不提供执行时重新读取当前MediaController的接口。输入在构造时固定；标题/歌手一并捕获。直接小图直接投递；大图/URI/description备用来源走MediaArtworkWork独立有界媒体池；最终通过同一请求提交缓存及UI。原临时显示缓存图行为保留（replaceCache=false），它是加载期间占位显示，不能称为“所有时刻封面与歌曲严格对应”。URI全部失败保留原150ms一次重试，中断恢复标志并退出。

Java8编译和38项管线断言通过，使用真实异步生产调度/管线与图片、IO、UI替身。验证慢旧缩放晚于新直接图、四来源组合、失败重试次数、停止中断重试、读取期间会话/管理器失效与close、缩放异常及最终缓存/标题。测试中断仅动态证明不再重试；executor可能清中断位，不能以线程池之后的状态证明恢复标志。源码明确调用interrupt，后续DEX仍需核对。证据在modules/media_artwork_pipeline_r1/result.json，脚本stability_run_media_artwork_pipeline_jvm_20260912.py。

模块仍未接入APK，没有创建R21。Android metadata提取/提交桥接、三旧任务入口替换、原媒体executor移除与owner关闭、URI缓存/流/连接/上限仍待实现。discard仅释放引用，不recycle借用图片；它不是独占位图回收方案。现有URI读函数还会写HTTP缓存，不能在接入时忽略过期写入及引用归属。

接入检查发现R20把实际Application Context当Service的准入错误，已优先进入独立R20b修复与成品验证。R20历史通过已明确撤回其功能正确性结论。下一管线Host必须沿R20b实际UI callback owner获得Service，测试替身必须来自真实构造/赋值链，不能重新假设Context等于Service。

完整P1/P2/P3仍开放；无设备、网络或车辆操作。
