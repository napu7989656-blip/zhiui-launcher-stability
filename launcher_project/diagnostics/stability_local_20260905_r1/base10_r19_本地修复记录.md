# BASE-10 R19 HOME广播接收器（2026-09-11）

APK `D:/apk/zhiui-stability-local-base10-r19.apk`，SHA-256 `D4D70049B7840A0D27ED7564AAF4F7448CD3867B987296228DCDD8C0AEDCD762`，30,080,319字节。3增量/131累计文件；父R18及历史包保留。未实车，整体计划未完成。

同一Service重连时复用已注册HOME接收器；注册成功后才保存引用，异常保留空引用供下次重连重试，避免重复注册与失败状态误记为成功。广播到达和排队回调执行都核对Service及接收器身份；空Intent安全退出。已经销毁的Service不再进入重连流程重新写O0。保留HOME/最近任务原有筛选与关闭面板行为，其他重连初始化仍执行。

源码/成品各326组（10新增+316继承），覆盖重复注册、注册失败重试、7种身份/生命周期状态、广播原因筛选与旧回调。异常范围与实际register调用已绑定核对；框架调用仍是替身。构建、对齐、开发签名、解析、独立回解、精确父恢复、131累计文件和22保护APK通过，Android29候选127无新增。JADX三次退出3均仅报告既有Ε方法错误；td.run默认省略，另行部分反编译case6与最终smali守卫一致。未将部分反编译或本地检查当ART/实车通过。

本轮重连审计的新事实：Service线程池在onCreate创建，1核心/最多3线程、32等待队列，并非每次重连重建。onServiceConnected会重建qi影像叠层并重投llI初始化；qi构造器缓存相机叠层偏好，不能未经配置刷新审计就直接复用。llI selector15每次创建a9媒体管理器，selector16创建BYD媒体管理器，selector17再次启动ADB初始化。a9拥有newSingleThreadExecutor；全smali直接字段引用仅构造赋值和submit读取，所见销毁路径取消Future/移除监听但不shutdown此池。两个提交点位于a9元数据处理，分别构造v8和w8；完整选择器、图片/歌词任务与回调仍待下一轮核对。不能据此宣布全部媒体生命周期已分类。

继续顺序：媒体管理器重复创建、线程池/监听/旧结果的完整关联链 → 影像叠层重连配置与资源 → ADB/IME/其他初始化 → 其余P1与完整V2/V3回引、P3总体门禁。旧Service全局静态清理的身份问题也未关闭。

未来本地门禁后由用户安全停车验证：打开面板后按HOME/最近任务，确认正常关闭；Service重连后动作正常且日志无重复注册/泄漏警告，采集NavBarSvc、ActivityManager和crash日志。失败先回原车桌面保留证据，再核对签名版本配置后恢复已验证V2。当前无设备操作、无安装、无正式交付目录写入。
