# R17 初始化内部审计（2026-09-11）

本轮是发现与复现，尚未生成R18补丁、工作树或APK。当前候选仍是R17，完整P1/P2/P3未完成。

## 已核对的调用链

NavBarService.onServiceConnected → Handler投递llI selector17 → ea selector0 → 所属Service线程池。ea在主连接成功后调用静态Λ（输入法设置）、实例Κ（通知监听设置），再投递m9 selector9（原电池后台运行许可请求）和8（CoreService启动）。Κ又投递m9 selector11（2/4/8秒后缺少MediaController时刷新会话）和12（3秒后扫描现有通知并刷新会话）；ea的m9 selector0更新导航界面。当前smali中Λ和Κ各只有ea的一处直接调用。

m9入口已有捕获Service的销毁检查，但不比较O0身份；selector8在耗时启动后又检查销毁才投递selector22，仍未比较当前实例。这些分支用途不同，不能把所有m9当作可替换只读状态而统一丢弃。

## 已确认问题和边界

1. **授权成功误判已复现。** c0.ˋ(ILjava/lang/String;)在普通异常路径返回空字符串；Κ的allow_listener判定只检查非null且不以HULU_ERROR开头，因此把该空字符串判为成功。audit_init_failure_contract_20260911.py执行实际smali判断片段，记录null、错误哨兵、异常空字符串和注入权限错误四种输入。空字符串使用非null Java引用语义，避免Python空串布尔值导致假阴性。空串也可能是成功命令的正常无输出，后续不能简单据空串决定成功/失败，需核对真实授权状态。
2. **读取阻塞先于所谓超时。** Κ的Runtime settings get分支先BufferedReader.readLine，再调用2秒waitFor包装器；readLine无超时，读不到换行/EOF时后面的等待上限不起作用。该流只在正常路径close，三处Runtime进程均无统一finally清理。
3. **等待结果丢失与中断继续。** g0.Ζ/Ι调用3秒/2秒Process.waitFor却丢弃boolean，之后exitValue可能抛异常；Κ的1秒sleep被普通Exception捕获，中断标志未恢复，随后还会继续授权和投递。不能把已有timeout参数当完整操作上限。
4. **未知读取可能覆盖列表。** settings get失败后会把null变成空串，再拼接本组件写回enabled_notification_listeners。后续必须区分“成功读取空列表”和“读取失败/未知”，未知时不能覆盖其他监听器；组件匹配应按分隔项核对，不能用子串contains代替精确组件匹配。这是静态路径风险，未执行真实设置写入。
5. **内部归属仍未覆盖。** Λ含多个顺序ADB设置/查询，Κ有多个备用步骤和媒体回调；R17检查点只控制进入/返回这些方法，不能阻止已进入方法内的后续步骤。需把捕获Service身份与中断贯穿内部流程和结果投递，保留既有用户行为及独立回滚。
6. **共享ADB层另有待审计路径。** c0.execAdbCmd主线程分支Future.get无显式超时且吞中断；后台层普通异常也会把中断变成空串。该层被其他用户操作共用，不能未经全调用方分类就更改全局取消、重连或错误返回约定。

## 证据与后续实现要求

base10_r17_nested_init_findings.json记录五个实际源文件SHA256与四个判断场景。base10_r17_nested_init_review.java为JADX独立部分输出，进程退出1且报告1个错误，不能称反编译全通过；Λ/Κ相关流程与apktool smali交叉确认。此前ea部分反编译错误记录继续有效。

下一修改优先覆盖通知监听的结果判定、未知列表保护和Runtime读取/进程清理，并为Service失效、中断、超时、异常空结果、其他组件保留及精确匹配建立回归；输入法与后续m9归属随后连同完整调用链收尾。若提取自有模块，应限于这些复杂操作的可维护封装，保留原APK架构、原命令目的和恢复路径。正在执行的外部动作不能因本地超时被假定已撤销。

本轮未执行ADB、权限变更、进程命令、安装或车辆操作。未实车，不进入正式交付目录。以上发现未修复前不得将R17或整体计划标记为完成。
