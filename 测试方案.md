# 智UI Launcher 零修改重打包实车测试方案

文档日期：2026-08-25  
测试对象：比亚迪海豚 2021 款时尚版  
车机版本：`13.1.22.2409213.1`  
当前已安装版本：最新版 `zhiui-final-v2.apk`  
待测版本：`Launcher_Rebuild_Test.apk`

## 1. 测试目的

本次只验证以下流程是否成立：

```text
原始 APK
→ apktool 无功能修改解码/重建
→ zipalign
→ 项目开发证书签名
→ 覆盖安装到实车
→ Launcher 正常启动、显示、恢复和回滚
```

本次不是功能验收，不修改 UI，不验证任何车辆写控制。只有本测试全部通过后，项目才进入后续单项功能修改阶段。

## 2. 测试包与回滚包

### 待测包

- 文件：`D:\apk\Launcher_Rebuild_Test.apk`
- packageName：`com.byd.launcher`
- versionName / versionCode：`1.0` / `1`
- 大小：30,063,935 bytes
- SHA-256：`FF06F8215156DC6CDA888C699B42FD5706A7E5E44E97227A0340A4EEA24DAAE9`
- 签名证书 SHA-256：`c2ba875afbf2efbbfff44cb64d4d5e16dd4cb1fef56e441fc8c3bde019fdb13c`
- 签名校验：V2=true、V3=true

### 回滚包

- 文件：`D:\apk\zhiui-final-v2.apk`
- packageName：`com.byd.launcher`
- versionName / versionCode：`1.0` / `1`
- SHA-256：`ADA7BD1DA144470F8C05BD24F0A7D8A27D00C435C0146D517F455D9F112FF1DC`
- 签名证书 SHA-256：与待测包相同
- 状态：最新版，既有实车验证通过

两个 APK 的包名、versionCode 和开发签名完全相同，可以用 `adb install -r` 原位覆盖并保留应用数据。

重要：安装待测包后，当前 v2 的新增界面和功能会暂时被原始基线实现替换。这是预期现象，不代表数据已删除。重新覆盖安装 `zhiui-final-v2.apk` 即可恢复 v2 代码和资源。

## 3. 安全红线

- 车辆必须安全停放，挂 P 挡并拉起驻车制动；测试期间不得驾驶。
- 不得点击或测试 HDC、驾驶模式、能量模式、门锁、后备箱、车窗、天窗、座椅、灯光、空调或全景相机控制。
- 只允许观察胎压、续航、电量、温度、里程、音乐状态等只读信息。
- 不得连接、扫描或探测 TCP 8888/8889，不得构造或发送任何 JSON/触摸命令。
- 不得执行 `adb uninstall`、`pm clear`、`pm disable-user`、`set-home-activity`、root、OTA 或系统分区修改。
- 不使用 `adb reboot`。重启测试只允许在前面项目全部通过后，用车机正常关机/重启流程执行。
- 若通过网络 ADB 测试，只使用隔离且可信的本地网络；不要让车机接入公共或不可信 Wi-Fi/LAN。

## 4. 测试准备

### 4.1 文件确认

在电脑 PowerShell 中执行：

```powershell
Get-FileHash -Algorithm SHA256 -LiteralPath D:\apk\Launcher_Rebuild_Test.apk
Get-FileHash -Algorithm SHA256 -LiteralPath D:\apk\zhiui-final-v2.apk
```

结果必须分别等于本文第 2 节记录的两个 SHA-256。任何一个不一致都停止测试。

### 4.2 ADB 变量和设备确认

```powershell
$ADB = 'C:\Users\L\AppData\Local\Android\Sdk\platform-tools\adb.exe'
& $ADB devices -l
```

确认列表中只有预期车机，或记下车机序列号/网络地址：

```powershell
$DEVICE = '<从 adb devices 获取的车机序列号或 IP:5555>'
& $ADB -s $DEVICE get-state
```

预期输出：`device`。

如显示 `unauthorized`、`offline`、空列表或存在多个无法确认的设备，停止测试，不执行安装。

### 4.3 测试前记录

- 拍摄当前 v2 主界面、应用抽屉和设置页截图。
- 记录当前壁纸、Dock 排列、Widget 排列和主要设置。
- 确认 `zhiui-final-v2.apk` 本地文件可以读取。
- 如需采集完整日志，另开一个 PowerShell 窗口执行：

新窗口不会继承前一个窗口的变量，请先在新窗口重新设置 `$ADB` 和 `$DEVICE`，再执行：

```powershell
$ADB = 'C:\Users\L\AppData\Local\Android\Sdk\platform-tools\adb.exe'
$DEVICE = '<从 adb devices 获取的车机序列号或 IP:5555>'
& $ADB -s $DEVICE logcat -v threadtime > D:\apk\Launcher_Rebuild_Test_logcat.txt
```

测试结束后在该窗口按 `Ctrl+C` 停止记录。不要先执行 `logcat -c`，以免清除已有诊断信息。

## 5. 安装与测试顺序

必须按顺序执行。某项失败后停止后续测试，转到第 7 节回滚。

### T01：覆盖安装

执行：

```powershell
& $ADB -s $DEVICE install -r D:\apk\Launcher_Rebuild_Test.apk
```

预期：

- 输出 `Success`。
- 不出现 `INSTALL_FAILED_UPDATE_INCOMPATIBLE`、`INSTALL_FAILED_VERSION_DOWNGRADE`、解析错误或 ABI 错误。
- 不执行卸载，不清除数据。

记录：

- 结果：通过 / 失败
- 安装输出：
- 耗时：

### T02：显式启动主界面

执行：

```powershell
& $ADB -s $DEVICE shell am start -W -n com.byd.launcher/.MainActivity
```

预期：

- Activity 启动成功，无黑屏、闪退或持续重启。
- 允许短时间初始化，但不应长时间停留在空白界面。
- logcat 无持续重复的 `FATAL EXCEPTION`、`VerifyError`、资源找不到或 native library 崩溃。

### T03：主界面视觉检查

只观察，不操作车辆控制：

- 壁纸正常显示，无拉伸、纯黑或资源缺失。
- 时钟和日期正常。
- 顶部状态栏与底部 Dock 正常出现。
- 温度/风量提示层没有异常常驻。
- 无明显错位、透明异常、闪烁或反复重新加载。
- 触摸空白区域不会崩溃。

预期：整体应接近原始基线界面，而不是最新版 v2 的全部新增效果。

### T04：应用抽屉

- 点击“全部应用”。
- 检查应用图标、名称、6 列分页和分页指示器。
- 左右切换两页。
- 只短按启动一个普通、非车辆控制应用，然后返回 Launcher。

禁止长按应用图标，避免触发卸载或投屏菜单。

预期：抽屉正常打开/关闭，普通应用可启动，返回后 Launcher 不重载或崩溃。

### T05：Home 行为

- 从一个普通应用按车机 Home 键。
- 重复两次，间隔至少 3 秒。

预期：

- 返回 `com.byd.launcher/.MainActivity`。
- 不弹出异常 HOME 选择器。
- Dock/状态栏正常恢复。
- 不跳到其他桌面，不出现黑屏。

如果跳转到其他桌面，只记录现象，不执行 `set-home-activity` 或禁用其他 Launcher。

### T06：熄屏/亮屏恢复

- 使用车机正常方式熄屏。
- 等待约 10 秒后正常亮屏。
- 重复一次。

预期：

- Launcher 能恢复。
- Dock/状态栏覆盖层能够恢复。
- 不出现持续的无障碍开关提示、反复 force-stop 或循环闪屏。
- 不主动操作任何车辆控制控件。

### T07：普通媒体显示

- 打开一个普通音乐应用并正常播放一首测试音频。
- 返回 Launcher，仅观察歌名、歌手、封面和播放状态。
- 正常暂停/继续音乐，可从音乐应用内操作。

预期：Launcher 不崩溃；媒体信息能够显示或保持合理的空状态。不要使用仪表投屏或车辆媒体写入相关高级功能。

### T08：只读车辆状态观察

只观察已有卡片上的数据：

- 电量/续航
- 温度
- 里程/行程
- 胎压
- 门/后备箱状态图标

预期：读取失败最多表现为空值、默认值或日志错误，不得导致 Launcher 崩溃。

禁止点击任何可能改变车辆状态的按钮。



只有 T01–T08 全部通过后才执行。

- 使用车机正常关机/重启流程。
- 不执行 `adb reboot`。
- 重启后等待系统稳定，不点击车辆控制。

预期：

- `com.byd.launcher` 自动成为/恢复为车机桌面。
- 壁纸、时钟、Dock、状态栏和应用抽屉正常。
- 不出现启动循环、黑屏或其他桌面抢占。

## 6. 通过标准

只有同时满足以下条件，才能判定“零修改重打包实车通过”：

- T01–T08全部通过。
- 没有持续 `FATAL EXCEPTION`、`VerifyError`、资源解析错误或 native 崩溃。
- Home、熄屏/亮屏和正常重启后都能恢复 Launcher。
- 应用抽屉和普通应用启动正常。
- 只读车辆状态不会导致崩溃。
- 未执行任何车辆写控制或端口测试。
- 不需要卸载、清数据、禁用系统应用或修改默认 HOME 才能运行。

构建成功、签名通过、能安装或能首次启动，单独都不能算完整通过。

## 7. 失败停止条件与回滚

任一情况发生时立即停止后续测试：

- 安装失败。
- 主界面黑屏或闪退。
- 出现持续崩溃/重启循环。
- Dock 或状态栏完全消失且不能恢复。
- Home 键无法回到 Launcher。
- 熄屏/亮屏后无法恢复。
- 无障碍服务反复开关、系统界面异常或影响正常车机操作。
- 任何意外车辆状态变化。

### 覆盖回滚到最新版 v2

保持应用不卸载、不清数据，执行：

```powershell
& $ADB -s $DEVICE install -r D:\apk\zhiui-final-v2.apk
```

预期输出：`Success`。

然后显式启动：

```powershell
& $ADB -s $DEVICE shell am start -W -n com.byd.launcher/.MainActivity
```

检查最新版 v2 主界面、Dock、Widget、壁纸和原有设置是否恢复。

如果覆盖回滚也失败：

- 不要执行 uninstall、pm clear、disable-user、root 或默认 HOME 修改。
- 保留车机当前画面，不继续尝试随机命令。
- 保存安装输出、截图和 logcat，交回工程人员处理。

## 8. 失败日志采集

优先提供完整的 `Launcher_Rebuild_Test_logcat.txt`。如未提前记录，可在故障出现后执行：

```powershell
& $ADB -s $DEVICE logcat -d -v threadtime > D:\apk\Launcher_Rebuild_Test_failure_logcat.txt
```

同时记录：

- 故障发生在哪个测试编号。
- 故障前最后一个操作。
- 车机画面截图或视频。
- 安装命令完整输出。
- 是否仍能进入其他系统页面。
- 回滚命令完整输出和回滚结果。

重点日志关键词：

- `AndroidRuntime`
- `FATAL EXCEPTION`
- `VerifyError`
- `Resources$NotFoundException`
- `LauncherApp`
- `MainActivity`
- `BootReceiver`
- `NavBarSvc`
- `CoreService`
- `BYD*DeviceManager`

## 9. 测试结果交接模板

```text
测试日期：
测试人员：
车辆/车机版本：比亚迪海豚 2021 时尚版 / 13.1.22.2409213.1
测试前安装版本：zhiui-final-v2.apk
待测 APK SHA-256：

T01 覆盖安装：通过 / 失败
T02 显式启动：通过 / 失败
T03 主界面：通过 / 失败
T04 应用抽屉：通过 / 失败
T05 Home：通过 / 失败
T06 熄屏/亮屏：通过 / 失败
T07 媒体显示：通过 / 失败
T08 只读车辆状态：通过 / 失败


是否发生车辆状态变化：否 / 是（立即说明）
是否执行过车辆控制：必须为否
是否执行过 8888/8889 测试：必须为否
是否回滚：否 / 是
回滚结果：
截图路径：
logcat 路径：
其他现象：

最终结论：零修改重打包实车通过 / 不通过
```

实车结果交回后，由工程人员更新 `PROJECT_MAP.md`。只有明确判定通过，才开始下一个单项修改版本。
