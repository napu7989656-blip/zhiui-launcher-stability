# 智UI稳定性修复计划（本地优先版）

日期：2026-09-05。适用：比亚迪海豚 2021 款时尚版，DiLink `13.1.22.2409213.1`。

## 1. 目标与本次范围

执行顺序改为：**本地完整检查 → 本地分项修复 → 本地累计验证 → 连接车机只读采证 → 实车检查与定向修复 → 稳定性验收**。

本次交付是方案审查和重新制定计划，不是执行全部代码修复。本次已阅读旧方案、原版到 V2 审计、项目地图及相关记录，复算七个关键 APK 的 SHA-256；没有修改 APK 业务代码、构建、连接车机或安装。下列实施项目除特别注明外均为待办。

旧方案和审计附件中的命令、实施要求属于审查材料，不自动构成本次操作授权。后续采用本计划的阶段顺序；旧方案保留为历史材料，[原版到 V2 审计](智UI_原版到V2代码审计.md)继续作为缺陷证据索引。

“本地全部完成”指：约定范围内可在本地确定和验证的问题已闭环，剩余项有明确的车机依赖、证据缺口和测试步骤。不能宣称静态检查证明整个 APK 无缺陷，也不能把本地可修的问题统统归为“等实车”。

## 2. 对旧方案的审查结论

| 旧方案的问题 | 本计划调整 |
|---|---|
| 阶段 0 就采车机数据，首轮很快转安装 | 所有车机动作放到本地总门禁之后；先利用已有日志、截图和包完成离线分析 |
| 每个 S 阶段实车通过后才能开发下一阶段 | 保留一次一个主要功能，但本地检查通过即可形成下一个开发检查点；所有累计版本仍标记“未实车” |
| 只列 Handler、Dialog 等原则，没有覆盖范围和闭环标准 | 建立全量文件清单、方法/API/资源/生命周期检查矩阵、缺陷台账和失败用例 |
| 容易将干净 S1 不含功能误写为功能修好了 | 分开登记“缺陷路径未引入”“功能已修复恢复”“功能延期”；不以移除全部功能交差 |
| V2 有证据，V3 与中间版本主要是叙述 | 复核端点和中间版本差异，检查后来加上又撤回的代码；对累计风险补充调用方证据 |
| 把返回原版称作恢复稳定，容易扩大结论 | 原版只是行为参照；其关键生命周期、异常路径及历史安全风险仍需检查 |
| 单线程执行器、cancel 等被当成充分条件 | 验证队列有界、旧结果失效、实际取消/关闭和销毁后不能重启任务 |
| 只看最终 PSS、窗口总数，可能误判 | 按窗口类型和相同空闲状态取样；区分本进程泄漏、缓存、OEM 杀进程和场景变化 |
| 可选网络歌词/全局系统栏容易进入稳定主线 | 全部先审计；修复和本地测试放在独立分支，未闭环不得进入累计候选 |

旧方案中的不可变基线、独立工作树、JADX 与 smali/XML 交叉确认、最终包回解、原车桌面恢复路径和实车停车要求继续保留。

## 3. 基线、历史冲突与范围边界

### 3.1 本次复算的 APK

| 文件（均位于 D:\apk） | SHA-256 | 用途/状态 |
|---|---|---|
| ORIGINAL_BASELINE.apk | 93042E48C12ED395E8F5D450BE1285E0397897F685EE95AC4080763B6E6DCCD9 | 权威只读基线 |
| original.apk | 93042E48C12ED395E8F5D450BE1285E0397897F685EE95AC4080763B6E6DCCD9 | 同字节备份 |
| 智UI_V0.4.2.apk | 93042E48C12ED395E8F5D450BE1285E0397897F685EE95AC4080763B6E6DCCD9 | 同字节备份 |
| Launcher_Rebuild_Test.apk | FF06F8215156DC6CDA888C699B42FD5706A7E5E44E97227A0340A4EEA24DAAE9 | 零修改换签参照 |
| zhiui-final-v2.apk | ADA7BD1DA144470F8C05BD24F0A7D8A27D00C435C0146D517F455D9F112FF1DC | 已验证功能回滚成果，不代表所有路径无缺陷 |
| zhiui-final-v3.11-lyrics-click-music-app-r1.apk | 216432903EDFD70E9F774C6EF9B1379BD3ABC9D80C52E8B7D79386EDE3698710 | 累计功能和缺陷参照 |
| zhiui-stability-s1-systembars-r1.apk | 3115734924E1D5C5FF5BFA516CB577394B53228060F4CE492969A38A0B4FF790 | 既有静态通过候选，未实车；保留不覆盖 |

本次只复核上述文件哈希，未重新验签或重复完整代码审计。S1 的签名、包属性和关键文件一致性结论来自 PROJECT_MAP.md 第 32～33 节既有记录。

### 3.2 实施前先完成的记录核对

- `AGENTS.md` 第 3 节和 `launcher_project/README.md` 仍写零修改包待实车；PROJECT_MAP.md 第 14 节记有 T09/T10 用户确认，第 16 节明确记录 2026-08-27 用户确认零修改测试通过。第 14 节也保留了较早的未完成采样记录。应将“较早未采到日志”“后续用户确认”分开列明来源，不把缺少自动采样等同于没有用户确认。
- L0 归档上述确认记录及对应包哈希，核实安装、启动、HOME 和基本功能证据覆盖，形成门禁核对结论。本计划不擅自改写 AGENTS.md，也不要求仅因旧文字而重复实车。若核对确实不足以满足第 3 节，继续全部只读检查和修复设计，正式 UI/功能修改须先解决该门禁；不隐瞒这个前置条件。
- PROJECT_MAP.md 第 32 节标题“未构建”与后续签名记录、重复第 33 节属于历史记录结构问题；以具体时间和产物记录标注状态，不删除历史。
- `智UI交付规则.md` 的“只保留三包、删除旧包”与本工作区 AGENTS.md 的归档保留要求冲突；按 AGENTS.md 保留全部归档，不执行清理。未实车验证仍不得进入 `D:\output\zhiui\clear`。

### 3.3 实现边界

以已核验的原版/零修改解码树为参照，在独立工作树做可回滚增量修改，不整体移植 V3.11，也不重写 Launcher。保留 V2 功能清单及 V3 后续功能清单，逐项记录保留、修复、默认关闭或延期；涉及功能取舍时明确列出差异。

允许只读分析既有车辆接口、权限、8888 服务和生命周期。不得主动发送车辆控制指令，也不通过调用危险入口来做覆盖率测试。发现原版车辆控制服务风险应单独登记；需要改变控制能力的修复不得夹带进入稳定性补丁。无法隔离的严重风险必须在门禁中明确列为阻塞，不能用“原版就有”关闭问题。

## 4. 阶段 L0：冻结证据和建立完整覆盖清单（仅本地）

1. 为本轮建立唯一目录，例如 `D:\apk\launcher_project\diagnostics\stability_local_20260905_r1`；存在则另取新名。其下区分只读参考、各项工作树、检查结果、最终包回解、日志和回滚记录。
2. 记录所有相关 APK 的文件名、哈希、签名摘要、版本、来源和验证等级。原包、开发证书、截图和日志均保留；文档中不写口令。
3. 核验已有 apktool/JADX 树对应的输入 APK；不匹配或证据不全时在新目录重新解码。原始 APK 不用普通 ZIP 解压生成 Windows 修改树，专门检查大小写碰撞和 apktool 警告。
4. 对原版→V2、V2→V3.11、原版→S1 建立全部路径/哈希差异；全 smali 分片、混淆包 `smali/o`、Manifest、res、assets、native、unknown 均纳入清单。业务差异与重打包/图片编码差异分开统计。
5. 对本地保留的 V3 中间包生成相邻版本差异索引；重点追踪状态栏重挂、媒体 API、空调和全局策略的加入/撤回。端点未变化不代表中间从未修改。
6. 人工审查覆盖全部新增/修改业务方法，以及其调用方、生命周期结束路径、资源使用方；未改第三方库做版本/差异和相关调用检查，必要时扩展，不声称每个库方法都逐行证明。
7. 索引已有崩溃、ANR、rapidmode、窗口和媒体日志，绑定当时 APK/场景。无版本对应关系的日志只能作为线索，不能用于确认当前故障根因。

产物：`inventory.csv`、`file_diff.csv`、`method_coverage.csv`、`feature_matrix.md`、`issues.csv`、`baseline_gate.md`。以上为计划中的产物名，本轮尚未生成。

## 5. 阶段 L1：本地检查矩阵

先检查完整范围再确定补丁边界；检查结论必须指向 APK 哈希、文件、方法/资源和实际分支，不能只提交关键词搜索结果。

| 检查面 | 本地必须检查和处理 | 可留到车机的证据 |
|---|---|---|
| DEX / smali / API | 类型、字段、方法签名与声明类，invoke 类型、寄存器宽度、返回值、异常区间；新增调用对目标 Android API 的兼容；反射名称及失败分支；禁止复发 NoSuchMethodError | 厂商隐藏 API 的实际实现、权限及运行期验证 |
| 资源与布局契约 | inflate→findViewById→类型转换/调用链；include、布局变体、ID/public.xml 一致性；未修改使用方也检查；恢复原 vehicle_control_panel 的完整契约 | 目标密度、系统合成、真实壁纸画面和触摸区域 |
| HOME / 启动 / 服务 | Manifest 入口、默认 HOME 恢复路径、Intent 空值/包缺失、启动异常处理；无障碍绑定/解绑、服务多次创建与销毁 | OEM HOME 路由、开机恢复、rapidmode 行为 |
| 窗口和所有权 | 每类 View/Window 的创建者、添加状态、移除者；重复 show/dismiss/remove、异常后引用清空；设置窗口不能仅隐藏；检查延迟重挂路径 | WindowManager 实际窗口数、悬浮权限和服务重建 |
| 生命周期和并发 | Handler/同一 Runnable 取消，回调注册注销成对，Executor/Future/线程停止，静态强引用、旧对象回调、可见性和熄屏策略；晚到结果不得复活 | 真实前后台事件顺序、进程终止和系统调度 |
| 主线程负担 | UI/无障碍回调中的网络、磁盘、大图解码、反射循环、shell wait、长 Binder 调用/锁；移出或限界；关键异常限频保留原因 | 真实 Binder 延迟、帧卡顿、ANR 堆栈 |
| 壁纸 | 统一主题、目录回落、有效图片过滤、读取失败、解码失败、大图内存、任务唯一性；0/5/10/15 分钟配置和重排 | 存储授权、车机视频壁纸和渲染性能 |
| 媒体/歌词 | 会话切换与注销、空封面/标题复位、暂停/销毁停止；generation 标记使旧回调失效；歌词解析边界、响应和缓存上限 | 播放器真实元数据、会话提供能力和网络时延 |
| 输入/广播 | 先读开关/映射，单次按键和跨入口去重；空 Intent、错误 action、异常频率；exported 组件和发送方约束 | 厂商真实按键/广播来源，无法本地证明的权限 |
| 偏好与升级/回滚 | 从 V2/V3 留下的配置启动：类型错误、旧目录、旧包名、间隔越界、开关残留；向后兼容；不清数据解决问题 | 实际保留数据、签名/版本安装限制、HOME 和全局设置 |
| Manifest / ABI / native | 包名、组件、exported、权限、SDK、ABI 和库加载变化；不把 targetSdk 当作车机 API；核对已有 Android 10 记录及本地 SDK 参考 | 当前设备系统/API/ABI 最终确认和厂商 native 行为 |
| 构建及诊断脚本 | 固定工具绝对路径、唯一输出名、全量重编译、错误码处理、目录边界、凭据不落日志；采集脚本不得隐含安装/清日志 | 实际设备上命令可用性；不可用项如实记录 |

可达路径的类型/API 错误、布局必然空引用、未清理窗口、无界任务/网络、旧回调回写等明确缺陷必须先本地修复；不能留给用户反复实车找这些错误。

## 6. 阶段 L2：按单项补丁修复，全部先在本地完成

每一行是一个主要修复单元。每完成一项即做局部验证、独立构建和差异记录；不需要每项等待实车。新产物采用 `zhiui-stability-local-lXX-rN.apk` 等唯一名称，避免混用旧 S1 的验证记录。

| 顺序 | 修复单元与关联问题 | 本地完成标准 |
|---|---|---|
| L01 | 状态栏/导航栏所有权（V2-01、V3 P0-4） | 复用原版架构并审计清理链；无失效反射、入口 return 禁用和重复重挂；定义每类窗口预期数量。若与原版完全相同，记“恢复基线”，不虚构新增修复 |
| L02 | 原车控/车辆信息布局契约（V2-02） | 原 ID、类型和全部使用方匹配；将来新面板使用独立布局，不覆盖原资源 |
| L03 | 设置窗口释放（V2-03） | 改设置通过窗口所有者真正关闭并清空字段；重复关闭和创建失败路径可处理 |
| L04 | 壁纸目录策略（V2-07） | 初次加载、手动刷新、主题切换共用解析，缺失/空/非图片/损坏文件都有确定结果；只保留一套设置接线 |
| L05 | 壁纸轮播生命周期（V2-06） | 单一调度对象，关闭/不可见/销毁取消，改间隔先撤旧任务；旧 Activity 不被延迟保留或更新；实测可见场景策略另列 |
| L06 | 模式键/广播入口（V2-04、V2-05、V3 P1-2） | 自定义关闭/原功能/播放暂停/空调映射分别可推导；普通媒体窗口事件不直接当按键；跨入口去重；来源未证实的广播路径隔离待测 |
| L07 | 空调面板窗口生命周期（V2-05、V3 P0-3） | 当前 Service 拥有单实例，dismiss/销毁/失败清空，重复事件不叠窗或瞬开瞬关；只审查窗口与既有接线，测试替身不发送车控 |
| L08 | 本地媒体信息（V2-08） | 切播放器/空数据/销毁清旧图和文字；旧会话回调不能覆盖新会话；取消与重新注册成对 |
| L09 | 歌词刷新调度（V3 P0-1） | 独立 start/update/stop/reset；不可见/关闭/暂停/会话销毁停止 UI TICK；销毁后晚到回调无效，不周期反射查 Activity |
| L10 | 网络歌词请求（V3 P0-2） | 一个工作线程且队列有界，最多保留最新待查曲目；旧 Future 取消并断开连接，流在所有退出路径关闭；超时、响应大小、缓存、重试均有限；代际校验防止 A→B→A 旧响应覆盖新状态 |
| L11 | 全局系统栏策略（V3 P1-3，独立可选分支） | 主线不执行 policy_control 写入；隔离旧开关且保留残留检测提示。可选修复必须移走同步 shell、验证真实读回值、保存原值/缺失状态并定义精确恢复，不能靠盲删全局项 |
| L12+ | 应用启动、HOME 快捷入口、歌词点击及必要布局兼容 | 每项独立差异；包未安装/无启动入口不崩溃；保留沉默版高德设置与启动链的一致性；UI 变化不得暗中改变系统主题/窗口行为 |

L09/L10 可在独立本地分支先完成实现和失败用例，不再等 L08 实车后才开始本地检查；实际网络能力默认关闭，只有本地门禁通过才进入可选候选。L11 不与状态栏架构变化放在同一修复单元。普通跨应用状态栏的原版行为与“新设计的双层重挂”必须区分；不把恢复原功能当作新功能永久延期。

原版审计发现的其他稳定性缺陷按新编号加入同一台账，每项一个补丁。复杂并发或解析逻辑先评估自有小模块，用可编译源码维护并只接入必要 smali，不将 JADX Java 当源码，不扩展成重写 Launcher。

**依赖规则：**本地开发检查点可以尚未实车；它只能作为开发父版本，不能作为已验证回滚包。下游本地工作继承所有待实车项，不能把继承问题标为通过。

## 7. 阶段 L3：本地可运行测试与最终包核验

### 7.1 本地测试的适用边界

- 优先测试实际修改模块：目录选择、配置迁移、歌词解析、请求代际/取消及事件去重。提供缺失目录、非图片、错误偏好类型、连续切歌、旧请求晚回、暂停/销毁等输入，并验证输出和资源释放；不能仅写重复实现的断言。
- 可用测试时钟和受控假会话/假网络驱动顺序、超时和错误，避免每个 15 分钟任务都用真实等待。若实现为独立模块，执行该模块的源码测试；若仍为 smali，结合最终 DEX 检查和可行的运行环境，模型测试仅记为辅助证据。
- 本地 HTTP/会话测试使用隔离替身，不要求真实歌词服务可用，也不调用车辆 manager、8888/8889 或底层接口。
- 如已有兼容 Android 模拟器/隔离设备，可做实际 APK 安装、目标类验证和通用窗口/生命周期测试，明确指定模拟器序列号，先审查脚本动作。缺少 arm64/native/BYD 依赖时如实记录，不能用占位服务成功冒充真 APK 通过。
- `dilink-sim-lab/README.md` 明确它是浏览器 mock，没有厂商服务，APK 尚未实现其调试广播接收器。它不能替代本 APK 运行验证；不为本轮测试擅自把调试广播加入交付包。
- 本地测试设施在工作区/允许目录完成；确实受环境限制的运行用例登记到车机清单。静态检查、资源契约和可隔离逻辑检查不能因此一并跳过。

### 7.2 每个补丁的构建链

1. 父树快照与允许改动文件/方法清单，排除 build/dist 等生成目录单独记录。
2. 使用 `D:\apk\launcher_project\build.ps1` / `build.bat` 所管理的固定工具路径；先核实参数及输出行为，在独立树全量 apktool build，不依赖旧缓存，不用覆盖参数重写历史 APK。
3. zipalign → 项目开发证书签名 → apksigner 和 zipalign 检查；证书摘要与 V2 回滚包比对。记录签名方案对目标系统的适用结果，不仅记录“命令成功”。
4. aapt/apkanalyzer 核对包名、版本、HOME/启动组件、SDK、权限、ABI、资源与 DEX 可解析；解析成功不等于 ART 已验证全部调用。
5. **对已签名最终 APK** 独立 apktool 回解，结构变化或重要方法变更时补做 JADX；确认实际 DEX/XML 与预期补丁一致。
6. 父版本→候选和基线→累计候选双向检查，业务变化必须全部可归属；native/assets 和受保护车辆调用链无意外变化。工具格式化差异也需分类解释。
7. 保存哈希、构建/验签/回解日志、失败用例与修复后结果。只进行与改动有关且有判别力的测试；有新增变化/失败才重跑相应检查。

### 7.3 累计候选专项检查

- 功能开关分别开/关，以及状态栏＋壁纸、设置＋轮播、面板＋服务销毁、媒体＋网络取消等交互组合。
- 使用 V2/V3 配置样本检查候选升级和回滚读取兼容，不清数据绕过迁移。区分功能缺省值与用户已保存的值。
- 检查诊断日志是否限频、是否能区分窗口/任务所有者及会话代际；不输出账号、曲库隐私详情或凭据。
- 对网络功能检查“一个工作线程”与“没有残留请求”是两项不同要求；暂停是否同时取消网络必须在策略中明确，功能关闭/服务销毁一定停止并使结果失效。
- 记录每个进程状态下的窗口、任务、媒体回调预期数量。不同种类窗口不合并成“全应用只能一个”。

## 8. 本地完成门禁：通过后才进入车机阶段

以下全部有证据才标记 `LOCAL_PASS / 未实车`：

1. 覆盖清单中全部新增/修改业务方法、相关调用方、布局契约和关键原版生命周期已处理；遗漏或无法解析项有明确处置。
2. 所有可本地证明的崩溃、API 错误、页面必然失败、窗口/任务泄漏、无界并发和阻塞问题已修复，且有回归证据。
3. 本地能跑的有效用例已执行；运行不了的项有具体依赖原因及最小实车步骤，不笼统写“需实车”。
4. 累计最终包构建、签名、对齐、属性、回解和差异检查通过，核心功能清单逐项有状态。未恢复的用户功能公开列出，不能称为全部完成。
5. 未闭环严重问题不得进入车机候选；仅可隔离的可选功能留在独立分支并清楚登记。功能默认关闭不能代替修复不可隔离的后台路径。
6. 实车只能验证的事项已归档：厂商 API/权限、真实窗口合成、播放器数据、存储授权、OEM 回收、长期内存和真实安装/回滚兼容。
7. 备齐同证书回滚包、原车桌面恢复步骤、候选配置兼容说明和只读采集脚本。仅本地通过的中间包不得标为稳定回滚包。

严重性与证据状态分开：状态依次为“待复核 / 静态确认 / 已修复待本地验证 / 本地通过待实车 / 实车通过”；“缺陷未引入”“功能延期”“环境阻塞”另列，不冒充修复。

缺陷台账至少包含：编号、出现版本/哈希、用户影响、证据路径与方法、可达条件、关联调用方、修复检查点、测试结果、本地剩余项、实车用例、回滚点。优先级不是已确认崩溃的证明。

## 9. 阶段 V0：首次连接车机，只读采集

本地门禁通过后，再由用户在安全停车状态下确认目标车机，按当次授权连接。不得复用历史 IP 推定设备，也不得假定当前安装的是 V3.11。

先审查并复用 `D:\apk\launcher_project\capture_stability_baseline.ps1`，明确序列号、独立输出目录和有限采集时长；不得清空 logcat。采集当前包路径/版本/可取得的 APK 哈希与签名、系统 API/ABI/固件、HOME、服务/无障碍、PID、线程、窗口、媒体会话、内存及 policy_control 当前值，保存 main/system/events/crash 原始日志。

没有权限或工具不支持的数据记录缺失原因，不为取日志 root、开放服务、改系统设置或触发车控。内存快照和详细 dumpsys 以阶段点采集，避免高频诊断本身影响结果。

将当前故障先分为 Java 崩溃、ANR、native 崩溃、OEM 强停、窗口/触摸异常、媒体/壁纸停止；保留 PID、时间和用户表现。不能把所有 rapidmode 终止归为代码崩溃。

## 10. 阶段 V1/V2：实车检查和定向修复

### 10.1 安装前与最小冒烟

提交已完成本地验证的具体候选、哈希、功能差异及回滚步骤，在授权范围内覆盖安装。先核对当前签名、versionCode 和配置兼容；证书不同或降级被拒时停止，不能自动卸载、清数据或假定 `-d` 一定可回滚。

首轮使用累计候选，网络歌词默认关闭，全局系统策略写入不进入主线。依次检查：安装→启动→HOME→应用列表→普通应用往返→原车桌面恢复入口→设置窗口→原车辆信息页。页面检查不点击底层控制；任何后续写车辆控制的测试不属于本稳定性计划。

S1 和各本地检查点备作定位参照，需要时才按单项回退缩小问题，不要求用户为每个本地补丁重复一次完整长测。

### 10.2 功能与资源验收矩阵

| 场景 | 最小执行量 | 预期与采证 |
|---|---|---|
| HOME/普通应用/状态栏 | 往返 50 次，前台观察 30 分钟 | 图标持续更新，窗口分类数量与本地所有权表一致；无重复栏、残留触摸层；前后窗口及日志快照 |
| 设置与轮播间隔 | 打开→改间隔→重新打开 20 次 | 设置窗口关闭后回到基准，无隐藏窗口累积 |
| 壁纸生命周期 | 关闭观察 20 分钟；5 分钟模式至少两周期；各做前后台、主题切换 | 目录一致，后台/销毁策略符合设计，无旧页面更新；结合任务计数和日志 |
| 面板及入口 | 开关 50 次；模式键四配置；普通媒体窗口变化 | 面板 0/1，无误触发、叠窗或瞬关；只开关窗口，不点空调控制 |
| 媒体 | 切歌/暂停/恢复 20 次，换播放器/退出/空封面 | 清空旧状态、无重复回调；结合真实 media_session |
| 可选网络歌词 | 本地通过分支单独开启；快切 20 次、断网/恢复、超时/空响应 | 单工作线程、队列受限、旧结果失效；暂停/退出后 2 秒内无 UI TICK；不得以工作线程空闲仍存在误判请求在跑 |
| 熄屏/亮屏 | 10 次 | 无窗口残留，恢复正确；保留服务和任务状态 |
| 长时间资源 | 预热后，0/15/30/60 分钟及操作结束静置 5 分钟快照；最终候选停车条件允许时延长至 2 小时 | 同一状态比较 PSS/Java/Native/Graphics、线程、窗口；有增长趋势则加同场景周期验证，不用任意固定 MB 或强制 GC 判过 |
| 正常重启恢复 | 至少 1 次，最终候选目标 3 次，由用户正常操作或明确授权 | HOME、服务、配置和窗口恢复；不做异常断电测试 |

每项通过需日志/快照和用户可见表现共同支持。无 FATAL/ANR/WindowLeaked/BadToken、无无法解释的 PID 更换、无随重复操作增长的窗口/任务；PSS 一次未回落不直接判泄漏，持续增长必须定位且不得放行。

### 10.3 实车失败后的修复闭环

1. 记录具体包哈希、操作顺序、时间、截图、完整日志及前后资源状态；先保留恢复入口。
2. 循环闪退、无法回 HOME、不可关闭遮挡层、ANR、明显资源持续增长或任何意外车辆动作时停止测试，在已授权且技术可行的范围内回滚。
3. 回本地把新证据对应到一个缺陷，形成单项 rN 修复；重跑相关本地验证、构建/签名/回解及受影响的累计检查。
4. 实车只先复测失败场景和关联回归，通过后再恢复剩余矩阵；不继续叠加其他功能试图遮盖原因。
5. 若属 OEM 系统主动结束进程，检查恢复与持久化，不能靠反复自启对抗系统策略，也不扩展为车辆控制修改。

## 11. 回滚与最终交付

- 默认回滚参照仍是 `D:\apk\zhiui-final-v2.apk` 或本轮后来真正实车通过的版本；需排除新增功能时使用零修改参照。每次安装前确认当次签名、版本及数据向后兼容，不能仅凭证书相同承诺覆盖回滚一定成功。
- 不卸载原车 Launcher、不破坏恢复入口。卸载/清数据需要用户明确确认，不能作为常规修复步骤。
- APK 回滚不会自动恢复 policy_control。读取并保存原值和“原本不存在”状态；只有在需要且已授权时精确恢复，不能默认删除整个全局策略。系统设置恢复与 APK 回滚分别记录。
- 正式交付需完整实车矩阵和长期/正常重启回归通过；本地包留在诊断目录，未实车包不进入 `D:\output\zhiui\clear`，全部历史归档保留。
- 最终交付：唯一 APK、哈希与证书摘要、父版本/回滚包、功能差异、完整检查覆盖与缺陷台账、补丁和累计 diff、构建/签名/回解日志、本地用例结果、实车日志/快照/截图及未解决限制；同步 PROJECT_MAP.md 和项目日志。

## 12. 下一次实施从哪里开始

2026-09-08续作位置：当前检查点BASE-10 r7，基线门禁和已有分项证据沿用，不从L0重做。仪表缓存/结果与重试归属已修；下一步修磁贴的过期结果与缓存写入，并补仪表等待中断/调用方检查，再处理玻璃后台任务/中断、调用方覆盖及V2/V3功能回引，最后完成第8节总本地门禁。导航结果有效性与队列类别已修，细节见本地整改进度.md；BASE-10和全计划仍未完成。当前不运行ADB采集，不以实车尝试替代未完成的本地检查。

本计划完成不代表上述修复已完成；进入车机的标志是第 8 节门禁有逐项证据，而不是生成了一个签名 APK。


2026-09-09执行更新：BASE-10 r8/r8b磁贴结果/缓存与说明文字已完成分项本地门禁，点击后复查纳入逐磁贴有界队列。继续SOC滑条初始化/轮询、用户操作冲突及其余既定本地项目，尚未达到实车阶段。证据见launcher_project/diagnostics/stability_local_20260905_r1/base10_r8b_本地修复记录.md。


2026-09-09：BASE-10 r9/r9b SOC读取、拖动有效性及已有异步操作等待完成分项本地门禁；下一步主题A→B→A、导航操作、后台中断及其余既定本地工作。未进入实车阶段。
